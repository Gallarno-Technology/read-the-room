---
status: complete
phase: 8.1-allow-reason-context
source: 8.1-01-SUMMARY.md, 8.1-02-SUMMARY.md
started: 2026-04-03T15:30:00Z
updated: 2026-04-03T22:00:00Z
---

## Current Test

<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running server/service. Clear ephemeral state (temp DBs, caches, lock files). Start the application from scratch. Server boots without errors, any seed/migration completes, and a primary query (health check, homepage load, or basic API call) returns live data.
result: issue
reported: "remove token cache, lyric db, make setup, make auth, docker compose up — 401 Permissions missing on GET /v1/me/player. Token exists but only has scope: user-read-currently-playing user-modify-playback-state"
severity: blocker

### 2. Mild Language Badge Appears
expected: Play a track that contains mild profanity (severity >= 1). On the dashboard, you should see two badges side by side: the green "Passed" badge AND an amber "Mild language" badge. Both badges are visible simultaneously in the now-playing card.
result: pass

### 3. Clean Pass Shows Only Passed Badge
expected: Play a track with no profanity (severity = 0) that passes evaluation. The dashboard shows only the green "Passed" badge. No "Mild language" badge appears.
result: pass

### 4. Badge Clears on Track Change
expected: When the track changes, the "Mild language" badge (if present) disappears from the now-playing card. Only the evaluating/pending state badge should show during track change — no stale severity badge from the previous track.
result: pass

### 5. Page Hydration with Severity
expected: While a track with known mild language (severity >= 1) is playing and has already been evaluated, refresh the dashboard page. On load, the page immediately shows both "Passed" and "Mild language" badges without waiting for an SSE event.
result: pass

## Summary

total: 5
passed: 4
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Server boots without errors and primary API calls succeed after fresh setup"
  status: failed
  reason: "User reported: remove token cache, lyric db, make setup, make auth, docker compose up — 401 Permissions missing on GET /v1/me/player. Token exists but only has scope: user-read-currently-playing user-modify-playback-state"
  severity: blocker
  test: 1
  root_cause: "setup_auth.py requested wrong OAuth scope — sp.current_playback() calls GET /v1/me/player which requires user-read-playback-state, not user-read-currently-playing"
  artifacts:
    - path: "setup_auth.py"
      issue: "scope missing user-read-playback-state (line 45)"
  missing:
    - "user-read-playback-state added to scope string in setup_auth.py:45 — FIXED"
  debug_session: ""
