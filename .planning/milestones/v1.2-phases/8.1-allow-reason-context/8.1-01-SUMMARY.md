---
phase: 8.1-allow-reason-context
plan: "01"
subsystem: daemon
tags: [python, asyncio, sse, events, severity, now_playing]

# Dependency graph
requires:
  - phase: 06-daemon-sse-extensions
    provides: eval_result SSE event schema and _write_now_playing helpers
  - phase: 06-daemon-sse-extensions
    provides: severity variable already unpacked from content_checker.check() at line 248
provides:
  - severity integer (0-3) in every eval_result SSE event payload
  - severity integer (0-3) in every now_playing.json write
  - test_eval_result_severity_mild test proving propagation end-to-end
affects:
  - 8.1-allow-reason-context plan 02 (frontend — reads severity from eval_result and now_playing API)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "eval_result event dict always includes severity regardless of code path"
    - "severity=0 used as sentinel when no profanity check ran (fsm-off, paused)"

key-files:
  created: []
  modified:
    - daemon.py
    - tests/test_daemon_events.py

key-decisions:
  - "severity=0 for 5th-skip-pause and fsm-off paths — no profanity scan ran; consistent field presence chosen over optional field"
  - "severity=severity for auto-skip path — severity variable already in scope from check() at line 248"
  - "TDD order: severity assertions added to tests first (RED), then daemon.py updated (GREEN)"

patterns-established:
  - "All eval_result event dicts include severity field — frontend can rely on field always being present"
  - "All _write_now_playing calls after evaluation include severity field"

requirements-completed:
  - SC4

# Metrics
duration: 7min
completed: 2026-04-03
---

# Phase 8.1 Plan 01: Add Severity to eval_result Events and now_playing.json Summary

**Severity integer (0-3) propagated from content_checker.check() into all eval_result SSE events and now_playing.json writes across 8 call sites in daemon.py**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-04-03T15:11:15Z
- **Completed:** 2026-04-03
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Threaded `severity` field into all 4 eval_result event dicts (allow, 5th-skip-pause, auto-skip, fsm-off)
- Threaded `severity` field into all 4 _write_now_playing calls matching those same branches
- Added severity assertions to existing test_eval_result_passed, test_eval_result_skipped, test_eval_result_fsm_off
- Added new test_eval_result_severity_mild (severity=1, no xfail marker) — passes clean
- All 13 tests pass (1 passed + 9 xpassed + 3 healthcheck passed)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add severity field to all eval_result events and now_playing.json writes** - `6b47084` (feat)

**Plan metadata:** TBD (docs: complete plan)

_Note: TDD — tests updated first (RED), then daemon.py (GREEN)_

## Files Created/Modified

- `daemon.py` - Added `"severity": severity` or `"severity": 0` to 8 eval_result/now_playing call sites
- `tests/test_daemon_events.py` - Added severity assertions to 3 existing tests; added new test_eval_result_severity_mild

## Decisions Made

- `severity=0` for 5th-skip-pause: the track was being evaluated for explicit content, not profanity; no profanity scan value available; 0 is the safe default matching "no profanity detected"
- `severity=0` for fsm-off: FSM disabled branch runs before `content_checker.check()` — severity variable is not in scope; literal 0 used
- `severity=severity` for auto-skip: the skip path runs after `check()` returns; the variable is in scope and is the actual measured severity

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None - the `severity` variable was correctly in scope for all branches where it was used.

## Known Stubs

None - severity is wired from `content_checker.check()` through to event payloads. Frontend plan (8.1-02) will consume this field.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 8.1-02 (frontend) can now read `severity` from `eval_result` SSE events and from `GET /now-playing` (which passes now_playing.json through as-is)
- No web_ui changes needed — D-05 confirmed: now_playing.json pass-through means severity arrives at frontend automatically

---

## Self-Check

- [x] daemon.py modified: `grep -n '"severity"' daemon.py` returns 8 lines
- [x] tests/test_daemon_events.py modified: new test_eval_result_severity_mild added
- [x] Commit 6b47084 exists
- [x] All 13 tests pass (1 passed + 9 xpassed + 3 healthcheck)
- [x] track_change event dict does NOT contain severity key

## Self-Check: PASSED

*Phase: 8.1-allow-reason-context*
*Completed: 2026-04-03*
