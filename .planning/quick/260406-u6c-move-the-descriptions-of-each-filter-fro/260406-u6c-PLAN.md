---
phase: quick
plan: 260406-u6c
type: execute
wave: 1
depends_on: []
files_modified:
  - web_ui/templates/index.html
autonomous: true
requirements: [U6C]

must_haves:
  truths:
    - "Each profile option in the dropdown shows its description beneath the profile name"
    - "The info icon popout shows 'Read the Room' heading and three static paragraphs regardless of active profile"
    - "The info popout no longer updates when the active profile changes"
  artifacts:
    - path: "web_ui/templates/index.html"
      provides: "Updated dropdown HTML with per-profile descriptions, updated info panel with static copy"
  key_links:
    - from: "profile-option divs"
      to: ".profile-option-desc spans"
      via: "static HTML subtexts inside each dropdown option"
    - from: "openInfo()"
      to: "#info-panel static content"
      via: "no longer builds list from PROFILE_INFO — renders static heading + paragraphs once"
---

<objective>
Move per-profile descriptions from the info icon popout into the dropdown menu as subtitle lines under each profile name. Repurpose the info icon popout as a static app overview with the heading "Read the Room" and three fixed paragraphs.

Purpose: Descriptions belong at the point of selection (dropdown) rather than in a separate popout. The info icon becomes a brand/philosophy overview, not a data sheet.
Output: Updated index.html with new dropdown subtitles and new static info popout content.
</objective>

<execution_context>
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/workflows/execute-plan.md
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add description subtitles to dropdown profile options</name>
  <files>web_ui/templates/index.html</files>
  <action>
In the `#profile-dropdown` div (around line 594–599), update each `.profile-option` div to include a description span beneath the profile name. Keep the existing `data-profile` and `role`/`aria-selected` attributes. Add a `<span class="profile-option-name">` wrapping the name text and a `<span class="profile-option-desc">` for the description. Final HTML for each option:

```html
<div class="profile-option" data-profile="kids_present" role="option" aria-selected="false">
  <span class="profile-option-name">Kids Present</span>
  <span class="profile-option-desc">Skips profanity, drug references, sexual content, and explicit-flagged tracks.</span>
</div>
<div class="profile-option" data-profile="were_all_adults" role="option" aria-selected="false">
  <span class="profile-option-name">We're All Adults</span>
  <span class="profile-option-desc">Skips profanity and sexual content.</span>
</div>
<div class="profile-option" data-profile="above_the_covers" role="option" aria-selected="false">
  <span class="profile-option-name">Above The Covers</span>
  <span class="profile-option-desc">Skips sexual content.</span>
</div>
<div class="profile-option" data-profile="permissive" role="option" aria-selected="false">
  <span class="profile-option-name">Permissive</span>
  <span class="profile-option-desc">Skips explicit-flagged tracks.</span>
</div>
```

Add CSS for `.profile-option-name` and `.profile-option-desc` in the `<style>` block near the existing `.profile-option` rules (around line 260):

```css
.profile-option-name {
  display: block;
  font-weight: inherit;
}

.profile-option-desc {
  display: block;
  font-size: 11px;
  color: var(--text-dim);
  font-weight: 400;
  margin-top: 1px;
}

.profile-option--active .profile-option-name {
  font-weight: 600;
}
```

The existing `::before` checkmark rule on `.profile-option--active` attaches to the option div, so the checkmark will precede `.profile-option-name` naturally — no change needed there. The `setFsmUI()` function toggles `profile-option--active` on the entire `.profile-option` div, which still works correctly.
  </action>
  <verify>Open the dropdown — each option shows its name on one line and a smaller dimmed description below it. The checkmark still appears on the active option.</verify>
  <done>All four dropdown options display name + description. Active option still shows checkmark and bold name.</done>
</task>

<task type="auto">
  <name>Task 2: Replace info panel content with static app overview</name>
  <files>web_ui/templates/index.html</files>
  <action>
**HTML change:** Replace the `<ul id="info-list" class="info-list"></ul>` inside `#info-panel` with static heading and paragraph content:

```html
<div id="info-panel" class="info-panel" hidden role="tooltip">
  <p class="info-panel-heading">Read the Room</p>
  <p class="info-panel-body">Your music doesn't change. The filter does.</p>
  <p class="info-panel-body">Set your House Rules for who's in the room. Read the Room handles the rest — quietly, in the background, without making a scene.</p>
  <p class="info-panel-body">Your house. Your standards.</p>
</div>
```

**CSS additions** (add near existing `.info-list` / `.info-entry` rules):

```css
.info-panel-heading {
  font-family: 'Playfair Display', serif;
  font-size: 13px;
  font-weight: 700;
  color: var(--text);
  margin: 0 0 8px 0;
}

.info-panel-body {
  font-family: 'Source Sans 3', sans-serif;
  font-size: 12px;
  color: var(--text-dim);
  margin: 0 0 6px 0;
}

.info-panel-body:last-child {
  margin-bottom: 0;
}
```

**JS changes:**

1. Remove `const infoList = document.getElementById('info-list');` (line ~668) — no longer needed.

2. Replace the entire `openInfo()` function body's lazy-build block (the `if (!infoList.hasChildNodes())` block, lines ~744–758) with nothing — the static content is already in the DOM. The function reduces to just the show logic:

```js
function openInfo() {
  infoBtn.setAttribute('aria-expanded', 'true');
  if (isMobile()) {
    infoPanel.removeAttribute('hidden');
    requestAnimationFrame(function() {
      infoPanel.classList.add('info-panel--open');
    });
    infoBackdrop.classList.add('active');
  } else {
    infoPanel.removeAttribute('hidden');
    infoBackdrop.classList.remove('active');
  }
}
```

3. The `PROFILE_INFO` map is no longer used anywhere after this change — remove it (lines ~679–684) to avoid dead code.

4. The `setFsmUI()` function does not reference `infoList` or `PROFILE_INFO` — no changes needed there. Confirm by checking the function body.

Also remove the now-unused CSS rules for `.info-list`, `.info-entry`, `.info-entry-name`, `.info-entry-desc` to keep the stylesheet clean.
  </action>
  <verify>Click the info icon — the popout shows "Read the Room" heading and the three paragraph body texts. The content is identical regardless of which profile is active.</verify>
  <done>Info popout shows static content only. No JS errors in console. Dropdown and FSM toggle still function normally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Dropdown profile options now show a description subtitle beneath each profile name
    - Info icon popout replaced with static "Read the Room" app overview (3 paragraphs)
    - Dead code removed (PROFILE_INFO map, infoList references, old info CSS classes)
  </what-built>
  <how-to-verify>
    1. Open the app in a browser
    2. Click the dropdown arrow on the FSM button — verify each option shows name + dimmed description; active option still has checkmark
    3. Switch profiles — verify checkmark moves correctly, names still bold
    4. Click the info icon (ⓘ) — verify the popout shows "Read the Room" heading and the three exact paragraphs
    5. Switch to a different profile, click info icon again — verify content is unchanged (static)
    6. On mobile (or narrow viewport ≤640px): click info icon, verify bottom-sheet slides up with the same static content
    7. Check browser console for JS errors — expect none
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Dropdown options render name + description without breaking existing profile selection or checkmark logic
- Info panel shows "Read the Room" + three exact paragraphs (verbatim from CONTEXT.md)
- No references to `PROFILE_INFO` or `infoList` remain in JS
- No JS console errors
- Mobile bottom-sheet still transitions correctly
</verification>

<success_criteria>
- All four dropdown options display description subtitles matching the exact sentences from CONTEXT.md
- Info popout content is the three exact paragraphs specified by the user, headed "Read the Room"
- Info popout content does not change when profile changes
- Existing FSM toggle, profile selection, and skip functionality unaffected
</success_criteria>

<output>
After completion, create `.planning/quick/260406-u6c-move-the-descriptions-of-each-filter-fro/260406-u6c-SUMMARY.md`
</output>
