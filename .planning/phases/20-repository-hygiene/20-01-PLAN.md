---
phase: 20-repository-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .dockerignore
  - .gitignore
autonomous: true
requirements:
  - HYG-01
  - HYG-02
must_haves:
  truths:
    - "A Docker build from the project root cannot bake .env or token_cache/ into the image"
    - ".claude/ directory is absent from git ls-files output"
    - ".claude/ is listed in .gitignore"
  artifacts:
    - path: ".dockerignore"
      provides: "Docker build context exclusions"
      contains: ".env"
    - path: ".gitignore"
      provides: "Git ignore rules including .claude/"
      contains: ".claude/"
  key_links:
    - from: ".dockerignore"
      to: "Dockerfile COPY . ."
      via: "Docker build context filtering"
      pattern: "^\\.env$"
    - from: ".gitignore"
      to: "git index"
      via: "git rm --cached -r .claude/"
      pattern: "\\.claude/"
---

<objective>
Create a .dockerignore at the repository root so Docker cannot bake secrets into image layers, and permanently untrack the .claude/ directory from git.

Purpose: Two irreversible credential exposure vectors — Docker COPY-all without dockerignore, and 217 tracked .claude/ files with absolute home paths — are closed before the repository goes public.
Output: .dockerignore created, .gitignore extended with .claude/, .claude/ removed from git index.
</objective>

<execution_context>
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/workflows/execute-plan.md
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-repository-hygiene/20-CONTEXT.md

<interfaces>
<!-- Key facts the executor needs. Extracted from direct source inspection. -->

From .gitignore (current state — 18 lines total):
```
# Secrets and runtime state — never commit
.env
state.json
token_cache/

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Docker
.docker/

# Runtime data and caches
data/
lyrics_cache.db
```
Note: .claude/ and .planning/ are NOT in .gitignore yet.

From CONTEXT.md (Pattern reference):
- .gitignore uses inline comments above each section explaining why entries are excluded
- git rm --cached MUST run AFTER .gitignore is edited (not before) — see Pitfall 3 in RESEARCH.md

From RESEARCH.md (verified facts):
- 217 .claude/ files are currently tracked by git
- Dockerfile and web_ui/Dockerfile both use `COPY . .` — root .dockerignore covers both (docker-compose.yml sets context: . for both)
- .dockerignore does not currently exist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .dockerignore</name>
  <files>.dockerignore</files>
  <read_first>
    - /home/cgallarno/Development/spotify-sentiment/Dockerfile (confirm COPY . . pattern)
    - /home/cgallarno/Development/spotify-sentiment/docker-compose.yml (confirm context: . for both services)
  </read_first>
  <action>
Create /home/cgallarno/Development/spotify-sentiment/.dockerignore with the following exact content (per D-07):

```
# Secrets and runtime credentials — must never be baked into image layers
.env
token_cache/

# Runtime state and data — container-managed, not source
state.json
data/
lyrics_cache.db

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Version control and dev tooling — not needed at runtime
.git/
.claude/
.planning/

# Test suite — not needed at runtime
tests/
```

This file must be placed at the repository root (same directory as Dockerfile and docker-compose.yml). The .dockerignore applies to both Docker services because docker-compose.yml sets `context: .` for both the daemon and web_ui services.

Do NOT add `.venv/` — that is already excluded by Python's standard .dockerignore conventions and may not exist in CI. The mandated minimum (per D-07) is: `.env`, `token_cache/`, `state.json`, `data/`, `lyrics_cache.db`, `__pycache__/`, `*.pyc`, `.git/`, `.claude/`, `.planning/`, `tests/`.
  </action>
  <verify>
    <automated>grep -E "^\.env$" /home/cgallarno/Development/spotify-sentiment/.dockerignore && grep "token_cache/" /home/cgallarno/Development/spotify-sentiment/.dockerignore && grep "\.claude/" /home/cgallarno/Development/spotify-sentiment/.dockerignore && grep "tests/" /home/cgallarno/Development/spotify-sentiment/.dockerignore</automated>
  </verify>
  <done>.dockerignore exists at repository root and contains .env, token_cache/, .claude/, and tests/ entries. All four grep checks return matches.</done>
  <acceptance_criteria>
    - `grep -E "^\.env$" .dockerignore` exits 0 (exact line ".env" present)
    - `grep "token_cache/" .dockerignore` exits 0
    - `grep "\.claude/" .dockerignore` exits 0
    - `grep "tests/" .dockerignore` exits 0
    - `.dockerignore` file exists at `/home/cgallarno/Development/spotify-sentiment/.dockerignore`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Untrack .claude/ from git</name>
  <files>.gitignore</files>
  <read_first>
    - /home/cgallarno/Development/spotify-sentiment/.gitignore (current state before modification)
  </read_first>
  <action>
STEP 1 — Edit .gitignore. Append a new section at the end of the file. The current .gitignore ends with the "Runtime data and caches" section (data/, lyrics_cache.db). Add this block after the last line:

```
# Dev tooling — personal config with absolute paths, not for public repos
.claude/
.planning/
```

The comment style matches the existing file: `# Category — reason` on the comment line, entries below.

STEP 2 — Run the git untrack command. After saving .gitignore, run this exact command from the repository root:

```bash
git rm --cached -r .claude/
```

This removes all 217 .claude/ files from the git index without deleting them from disk. The command will output one line per removed file — this is expected.

CRITICAL ORDER: Always edit .gitignore first, then run git rm --cached. Running git rm --cached before .gitignore is updated creates a window where the files are untracked but not gitignored, allowing git add . to re-track them.

After git rm --cached completes, verify immediately with:
```bash
git ls-files .claude/
```
This must return empty (no output).
  </action>
  <verify>
    <automated>git ls-files /home/cgallarno/Development/spotify-sentiment/.claude/ | wc -l</automated>
  </verify>
  <done>.claude/ is not listed in git ls-files output (count is 0). .gitignore contains .claude/ entry. Files still exist on disk in .claude/ directory.</done>
  <acceptance_criteria>
    - `git ls-files .claude/` returns empty (no output)
    - `grep "^\.claude/$" .gitignore` exits 0 (exact line ".claude/" present in .gitignore)
    - `grep "^\.planning/$" .gitignore` exits 0 (exact line ".planning/" present in .gitignore)
    - `.claude/` directory still exists on disk (files not deleted, only untracked): `ls .claude/` returns entries
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks complete, run the full Phase 20 Plan 01 acceptance suite:

```bash
# HYG-01: .dockerignore present and covers key exclusions
grep -E "^\.env$" /home/cgallarno/Development/spotify-sentiment/.dockerignore
grep "token_cache/" /home/cgallarno/Development/spotify-sentiment/.dockerignore

# HYG-02: .claude/ no longer tracked by git
git ls-files /home/cgallarno/Development/spotify-sentiment/.claude/
# must return empty

# .planning/ also untracked
git ls-files /home/cgallarno/Development/spotify-sentiment/.planning/
# must return empty (or only files intentionally tracked, but .claude/ must be gone)
```
</verification>

<success_criteria>
1. .dockerignore exists at repository root with .env, token_cache/, .claude/, tests/ all present
2. `git ls-files .claude/` returns empty — no .claude/ files in the git index
3. .gitignore contains .claude/ and .planning/ entries with explanatory comment
4. .claude/ directory still exists on disk (untracked, not deleted)
</success_criteria>

<output>
After completion, create `.planning/phases/20-repository-hygiene/20-01-SUMMARY.md` following the summary template at `.claude/get-shit-done/templates/summary.md`.
</output>
