---
phase: 17-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web_ui/templates/index.html
  - README.md
autonomous: true
requirements:
  - RBR-01
  - RBR-02

must_haves:
  truths:
    - "Browser tab displays 'Read the Room' as the page title"
    - "Dashboard <h1> heading reads 'Read the Room'"
    - "Incident Log empty state body copy references 'Read the Room', not 'Family Safe Mode'"
    - "README.md H1 reads '# Read the Room'"
    - "README.md intro sentence references 'Read the Room', not 'Family Safe Mode'"
    - "No occurrence of 'Family Safe Mode' or 'Spotify Family Safe Mode' remains in either file"
  artifacts:
    - path: "web_ui/templates/index.html"
      provides: "Updated display strings for RBR-01"
      contains: "Read the Room"
    - path: "README.md"
      provides: "Updated header and intro for RBR-02"
      contains: "Read the Room"
  key_links:
    - from: "web_ui/templates/index.html line 6"
      to: "browser tab title"
      via: "<title> tag"
      pattern: "<title>Read the Room</title>"
    - from: "web_ui/templates/index.html line 436"
      to: "visible dashboard heading"
      via: "<h1> tag"
      pattern: "<h1>Read the Room</h1>"
---

<objective>
Replace all user-visible occurrences of "Spotify Family Safe Mode" and "Family Safe Mode" with "Read the Room" in the two in-scope files.

Purpose: Complete the display-name rebrand so every string a user sees refers to "Read the Room."
Output: web_ui/templates/index.html and README.md with updated display strings.
</objective>

<execution_context>
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/workflows/execute-plan.md
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-rebrand/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.html display strings (RBR-01)</name>
  <files>web_ui/templates/index.html</files>

  <read_first>
    - web_ui/templates/index.html — read the full file before editing to confirm line context
  </read_first>

  <action>
Make exactly three string replacements in web_ui/templates/index.html. Do not touch any other lines.

Replacement 1 — line 6, `<title>` tag:
  FROM: `<title>Family Safe Mode</title>`
  TO:   `<title>Read the Room</title>`

Replacement 2 — line 436, `<h1>` tag:
  FROM: `<h1>Spotify Family Safe Mode</h1>`
  TO:   `<h1>Read the Room</h1>`

Replacement 3 — line 492, Incident Log empty state body copy:
  FROM: `<span class="empty-body">Skips will appear here in real-time when Family Safe Mode is on.</span>`
  TO:   `<span class="empty-body">Skips will appear here in real-time when Read the Room is on.</span>`

Do NOT run a project-wide find-replace. Edit only these three occurrences in this one file. Do not touch Python source files, Makefile, or any .planning/ document.
  </action>

  <verify>
    <automated>grep -c "Family Safe Mode" /home/cgallarno/Development/spotify-sentiment/web_ui/templates/index.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Family Safe Mode" web_ui/templates/index.html` returns `0`
    - `grep -n "Read the Room" web_ui/templates/index.html` returns matches on lines 6, 436, and 492
    - `grep -n "<title>Read the Room</title>" web_ui/templates/index.html` returns line 6
    - `grep -n "<h1>Read the Room</h1>" web_ui/templates/index.html` returns line 436
    - `grep -n "when Read the Room is on" web_ui/templates/index.html` returns line 492
  </acceptance_criteria>

  <done>All three occurrences of the old brand name are replaced; zero instances of "Family Safe Mode" remain in index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md display strings (RBR-02)</name>
  <files>README.md</files>

  <read_first>
    - README.md — read lines 1-5 before editing to confirm exact current text
  </read_first>

  <action>
Make exactly two string replacements in README.md. Do not touch any other lines.

Replacement 1 — line 1, H1 heading:
  FROM: `# Spotify Family Safe Mode`
  TO:   `# Read the Room`

Replacement 2 — line 3, intro sentence:
  FROM: `Automatically skips explicit songs when Family Safe Mode is on. Polls Spotify playback, checks lyrics, and skips via Sonos or the Spotify API.`
  TO:   `Automatically skips explicit songs when Read the Room is on. Polls Spotify playback, checks lyrics, and skips via Sonos or the Spotify API.`

Do not run a project-wide find-replace. Edit only these two occurrences in this one file.
  </action>

  <verify>
    <automated>grep -c "Family Safe Mode" /home/cgallarno/Development/spotify-sentiment/README.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Family Safe Mode" README.md` returns `0`
    - `grep -n "Read the Room" README.md` returns matches on lines 1 and 3
    - `grep -n "^# Read the Room$" README.md` returns line 1
    - `grep -n "when Read the Room is on" README.md` returns line 3
  </acceptance_criteria>

  <done>Both occurrences of the old brand name are replaced; zero instances of "Family Safe Mode" remain in README.md.</done>
</task>

</tasks>

<verification>
After both tasks complete, confirm the rebrand is clean across both files:

```bash
grep -rn "Family Safe Mode" web_ui/templates/index.html README.md
```

Expected output: no matches (command returns nothing, exit code 1).

```bash
grep -n "Read the Room" web_ui/templates/index.html README.md
```

Expected output: five matches — index.html lines 6, 436, 492 and README.md lines 1, 3.
</verification>

<success_criteria>
- RBR-01: `grep -c "Family Safe Mode" web_ui/templates/index.html` = 0
- RBR-01: `grep -c "Read the Room" web_ui/templates/index.html` = 3
- RBR-02: `grep -c "Family Safe Mode" README.md` = 0
- RBR-02: `grep -c "Read the Room" README.md` = 2
- No Python source files, Makefile, or planning documents were modified
</success_criteria>

<output>
After completion, create `.planning/phases/17-rebrand/17-01-SUMMARY.md`
</output>
