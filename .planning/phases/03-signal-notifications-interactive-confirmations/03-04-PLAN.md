---
phase: 03-signal-notifications-interactive-confirmations
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - daemon.py
  - web_ui/main.py
  - docker-compose.yml
autonomous: true
gap_closure: true
requirements:
  - FSM-03
  - SIG-01
  - SIG-02
  - SIG-04

must_haves:
  truths:
    - "Skip events written by the daemon process are received by the web_ui SSE endpoint in docker-compose mode (separate containers)"
    - "daemon.py appends a JSON line to data/skip_events.jsonl after every successful skip and after every five_skip_warning"
    - "web_ui/main.py tails data/skip_events.jsonl to populate the SSE source queue, replacing the broken in-process queue import"
    - "docker-compose.yml mounts data/ as a shared volume into both daemon and web_ui containers"
    - "Browser EventSource at /events receives skip events within 2 seconds of a daemon skip"
  artifacts:
    - path: "daemon.py"
      provides: "Appends JSON lines to data/skip_events.jsonl on every skip event"
      contains: "skip_events.jsonl"
    - path: "web_ui/main.py"
      provides: "File-tail coroutine that reads skip_events.jsonl and pushes events to SSE subscribers"
      contains: "skip_events.jsonl"
    - path: "docker-compose.yml"
      provides: "Shared ./data volume mount on both daemon and web_ui services"
      contains: "data/"
  key_links:
    - from: "daemon.py poll_loop"
      to: "data/skip_events.jsonl"
      via: "open(SKIP_EVENTS_PATH, 'a') then json.dump + newline"
      pattern: "skip_events\\.jsonl"
    - from: "web_ui/main.py _file_tail coroutine"
      to: "data/skip_events.jsonl"
      via: "asyncio file read with seek to end on startup, polling new lines"
      pattern: "skip_events\\.jsonl"
    - from: "docker-compose.yml daemon volumes"
      to: "docker-compose.yml web_ui volumes"
      via: "shared ./data bind-mount on both services"
      pattern: "\\./data"
---

<objective>
Replace the broken in-process asyncio.Queue IPC with a file-based event log. The daemon appends JSON lines to data/skip_events.jsonl; web_ui tails that file via a background asyncio coroutine and pushes new lines to SSE subscribers. Both containers share the file through a docker-compose volume mount.

Purpose: The asyncio.Queue import approach is silently broken in docker-compose (separate process memory). This is the fundamental reason the skip feed is empty and the 5-skip warning banner never appears in the deployed configuration. The file-based approach is the simplest fix that preserves the existing API contract (/events SSE, browser EventSource) without requiring a message broker.
Output: daemon.py writes skip events to data/skip_events.jsonl. web_ui/main.py tails that file instead of importing a queue. docker-compose.yml mounts data/ into both services.
</objective>

<execution_context>
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/workflows/execute-plan.md
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-signal-notifications-interactive-confirmations/03-CONTEXT.md
@.planning/phases/03-signal-notifications-interactive-confirmations/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add file-based event log writes to daemon.py</name>
  <files>daemon.py</files>

  <read_first>
    daemon.py — read lines 1–60 (imports, constants, module-level declarations) and lines 155–195 (the skip event emission block with skip_event_queue.put_nowait calls); the two put_nowait calls that need augmenting are at approximately lines 169 and 184
  </read_first>

  <action>
Add file-based event log writes alongside the existing skip_event_queue.put_nowait calls. The queue is NOT removed — it stays in place for compatibility. The file writes are additions, not replacements.

**Addition 1 — New constant after the existing LYRICS_DB_PATH constant (around line 36).**

Add immediately after the LYRICS_DB_PATH line:
  SKIP_EVENTS_PATH = os.environ.get("SKIP_EVENTS_PATH", "data/skip_events.jsonl")

**Addition 2 — Helper function for appending to the event log.**

Add this function immediately before the poll_loop function definition (after the module-level declarations, before "async def poll_loop"):

  def _append_skip_event(event: dict) -> None:
      """Append a JSON line to the skip events log (Gap-2 fix: file-based IPC for docker-compose)."""
      try:
          os.makedirs(os.path.dirname(SKIP_EVENTS_PATH) or ".", exist_ok=True)
          with open(SKIP_EVENTS_PATH, "a") as f:
              f.write(json.dumps(event) + "\n")
      except OSError as exc:
          log.error("[EVENTS] failed to write skip event log: %s", exc)

**Addition 3 — Call _append_skip_event after each existing put_nowait call.**

Locate the two existing skip_event_queue.put_nowait blocks in poll_loop:

Block A (skip event, around lines 169–175):
  skip_event_queue.put_nowait({
      "type": "skip",
      "track": track["name"],
      "artist": track["artists"][0]["name"],
      "reason": reason,
      "timestamp": time.strftime("%H:%M:%S"),
  })

Immediately after this put_nowait block, add:
  _append_skip_event({
      "type": "skip",
      "track": track["name"],
      "artist": track["artists"][0]["name"],
      "reason": reason,
      "timestamp": time.strftime("%H:%M:%S"),
  })

Block B (five_skip_warning, around lines 184–187):
  skip_event_queue.put_nowait({
      "type": "five_skip_warning",
      "timestamp": time.strftime("%H:%M:%S"),
  })

Immediately after this put_nowait block, add:
  _append_skip_event({
      "type": "five_skip_warning",
      "timestamp": time.strftime("%H:%M:%S"),
  })

Do not remove or change the existing put_nowait calls. Do not change any other part of daemon.py.
  </action>

  <verify>
    <automated>python -c "import ast; ast.parse(open('/home/cgallarno/Development/spotify-sentiment/daemon.py').read()); print('syntax OK')"</automated>
    <automated>grep -n "SKIP_EVENTS_PATH" /home/cgallarno/Development/spotify-sentiment/daemon.py</automated>
    <automated>grep -n "_append_skip_event" /home/cgallarno/Development/spotify-sentiment/daemon.py</automated>
    <automated>grep -n "skip_events.jsonl" /home/cgallarno/Development/spotify-sentiment/daemon.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('daemon.py').read())"` exits 0 with no SyntaxError
    - `grep "SKIP_EVENTS_PATH" daemon.py` returns at least 2 lines: the constant assignment and the open() call inside _append_skip_event
    - `grep "_append_skip_event" daemon.py` returns at least 3 lines: the function def, and 2 call sites (one for skip, one for five_skip_warning)
    - `grep "skip_events.jsonl" daemon.py` returns at least 1 line (the SKIP_EVENTS_PATH default value)
    - `grep -c "skip_event_queue.put_nowait" daemon.py` returns 2 (the original put_nowait calls are still present)
    - `grep "makedirs" daemon.py` returns 1 line (inside _append_skip_event)
  </acceptance_criteria>

  <done>daemon.py appends a JSON line to data/skip_events.jsonl after every successful skip and after every five_skip_warning event. The existing asyncio.Queue put_nowait calls are preserved for backward compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Replace in-process queue import with file-tail reader in web_ui/main.py and add shared volume to docker-compose.yml</name>
  <files>web_ui/main.py, docker-compose.yml</files>

  <read_first>
    web_ui/main.py — read lines 44–80 (the try/except import block that imports skip_event_queue from daemon, and the _broadcaster coroutine); this block must be replaced with a file-tail coroutine that reads skip_events.jsonl
    docker-compose.yml — read fully; must understand existing daemon volume mounts (state.json, token_cache, lyrics_cache.db) to add the ./data volume to both services without disturbing existing entries
  </read_first>

  <action>
**Change 1 — Replace the broken daemon import and broadcaster in web_ui/main.py.**

The current lines 44–79 contain:
  # (comment block)
  try:
      from daemon import skip_event_queue as _daemon_queue  ...
  except ImportError:
      _SOURCE_QUEUE = asyncio.Queue()
      ...
  _subscribers: list[asyncio.Queue] = []
  async def _broadcaster() -> None: ...
  @app.on_event("startup")
  async def _startup() -> None:
      asyncio.create_task(_broadcaster())

Replace the entire block from the first comment ("# Shared event queue") through the end of _startup() with the following. Keep everything before line 44 and after _startup() unchanged.

New block to replace lines 44–79:

  # ---------------------------------------------------------------------------
  # File-based IPC bridge — daemon writes skip_events.jsonl; we tail it here.
  # Replaces the broken in-process asyncio.Queue import (Gap-2 fix).
  # Both containers share the file via a docker-compose ./data volume mount.
  # ---------------------------------------------------------------------------
  SKIP_EVENTS_PATH = os.environ.get("SKIP_EVENTS_PATH", "data/skip_events.jsonl")

  # Each SSE client gets its own subscriber queue (maxsize=100).
  _subscribers: list[asyncio.Queue] = []


  async def _file_tail() -> None:
      """Tail skip_events.jsonl and push new JSON-line events to all SSE subscribers.

      Starts reading from the END of the file on startup (skips history) so the browser
      only sees events that occur while it is connected — consistent with the original
      in-process queue behaviour.
      """
      log.info("web_ui: tailing %s for SSE events", SKIP_EVENTS_PATH)
      # Wait until the file exists (daemon may start slightly after web_ui)
      while not os.path.exists(SKIP_EVENTS_PATH):
          await asyncio.sleep(1)

      with open(SKIP_EVENTS_PATH) as fh:
          fh.seek(0, 2)  # seek to end — skip existing history
          while True:
              line = fh.readline()
              if not line:
                  await asyncio.sleep(0.25)  # poll every 250 ms
                  continue
              line = line.strip()
              if not line:
                  continue
              try:
                  event = json.loads(line)
              except json.JSONDecodeError:
                  log.warning("web_ui: skipping malformed event line: %r", line)
                  continue
              dead = []
              for q in _subscribers:
                  try:
                      q.put_nowait(event)
                  except asyncio.QueueFull:
                      dead.append(q)
              for q in dead:
                  _subscribers.remove(q)


  @app.on_event("startup")
  async def _startup() -> None:
      asyncio.create_task(_file_tail())

No other changes to web_ui/main.py. The rest of the file (_sse_event_generator, sse_events, the FSM endpoints, the dashboard route) is unchanged.

**Change 2 — Add ./data volume mount to docker-compose.yml.**

Read the existing docker-compose.yml and add `- ./data:/app/data` to the volumes list of BOTH the daemon service and the web_ui service. The daemon service already has volumes; add the data mount at the end of its list. The web_ui service already has `- ./state.json:/app/state.json`; add the data mount after it.

The SKIP_EVENTS_PATH default in both daemon.py and web_ui/main.py is "data/skip_events.jsonl" which resolves to /app/data/skip_events.jsonl inside the container. The volume mount `./data:/app/data` makes this path shared between both containers and backed by ./data/ on the host.

Do not change any other part of docker-compose.yml.
  </action>

  <verify>
    <automated>python -c "import ast; ast.parse(open('/home/cgallarno/Development/spotify-sentiment/web_ui/main.py').read()); print('syntax OK')"</automated>
    <automated>grep -n "skip_events.jsonl" /home/cgallarno/Development/spotify-sentiment/web_ui/main.py</automated>
    <automated>grep -n "_file_tail\|from daemon import" /home/cgallarno/Development/spotify-sentiment/web_ui/main.py</automated>
    <automated>grep -n "data:" /home/cgallarno/Development/spotify-sentiment/docker-compose.yml</automated>
    <automated>grep -c "\./data" /home/cgallarno/Development/spotify-sentiment/docker-compose.yml</automated>
    <automated>grep -n "text/event-stream" /home/cgallarno/Development/spotify-sentiment/web_ui/main.py</automated>
    <automated>grep -n "family_safe_mode" /home/cgallarno/Development/spotify-sentiment/web_ui/main.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('web_ui/main.py').read())"` exits 0 with no SyntaxError
    - `grep "skip_events.jsonl" web_ui/main.py` returns at least 1 line (the SKIP_EVENTS_PATH constant)
    - `grep "_file_tail" web_ui/main.py` returns at least 2 lines: the function def and the asyncio.create_task call in _startup
    - `grep "from daemon import" web_ui/main.py` returns 0 lines (the broken import is removed)
    - `grep "ImportError" web_ui/main.py` returns 0 lines (the fallback try/except is removed)
    - `grep -c "./data" docker-compose.yml` returns 2 (one mount in daemon service, one in web_ui service)
    - `grep "text/event-stream" web_ui/main.py` returns 1 line (the SSE StreamingResponse media_type — unchanged)
    - `grep -c "family_safe_mode" web_ui/main.py` returns at least 4 lines (FSM endpoints unchanged)
    - `grep "_subscribers.append" web_ui/main.py` returns 1 line (sse_events subscriber registration — unchanged)
    - `grep "fh.seek(0, 2)" web_ui/main.py` returns 1 line (seeks to end of file on startup)
    - `grep "asyncio.sleep(0.25)" web_ui/main.py` returns 1 line (the 250ms poll interval)
  </acceptance_criteria>

  <done>web_ui/main.py tails data/skip_events.jsonl via _file_tail coroutine instead of importing a queue from daemon. docker-compose.yml mounts ./data into both daemon and web_ui containers so the file is shared. The SSE API contract (/events endpoint, browser EventSource) is unchanged.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -c "import ast; ast.parse(open('daemon.py').read())"` exits 0
2. `python -c "import ast; ast.parse(open('web_ui/main.py').read())"` exits 0
3. `grep "_append_skip_event" daemon.py` returns at least 3 lines
4. `grep "from daemon import" web_ui/main.py` returns 0 lines
5. `grep "_file_tail" web_ui/main.py` returns at least 2 lines
6. `grep -c "./data" docker-compose.yml` returns 2
7. `grep "text/event-stream" web_ui/main.py` returns 1 line (SSE contract preserved)
8. `grep "family_safe_mode" web_ui/main.py | wc -l` returns >= 4 (FSM endpoints unchanged)
</verification>

<success_criteria>
- daemon.py appends JSON lines to data/skip_events.jsonl; _append_skip_event is called after both the skip event and the five_skip_warning event
- web_ui/main.py uses _file_tail() coroutine to tail skip_events.jsonl; the broken "from daemon import skip_event_queue" try/except block is fully removed
- docker-compose.yml mounts ./data:/app/data into both daemon and web_ui services
- The existing SSE /events endpoint, FSM toggle, FSM status, and dashboard route in web_ui/main.py are fully preserved
- End-to-end: daemon skip → file append → file tail → SSE subscriber queue → browser EventSource event
</success_criteria>

<output>
After completion, create `.planning/phases/03-signal-notifications-interactive-confirmations/03-04-SUMMARY.md`
</output>
