---
phase: 03-signal-notifications-interactive-confirmations
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - web_ui/templates/index.html
  - web_ui/Dockerfile
  - docker-compose.yml
  - .env.example
  - Makefile
autonomous: true
requirements:
  - SIG-02   # Skip feed visible in Web UI (replaces Signal skip notification readback) — D-06, D-07
  - SIG-03   # FSM toggle interactive control in Web UI (replaces Signal reply allow/skip) — D-09
  - FSM-03   # Five-skip warning banner shown via SSE five_skip_warning event — D-12

must_haves:
  truths:
    - "Visiting http://localhost:8888 in a browser renders the dashboard with FSM toggle and Incident Log section"
    - "FSM toggle button shows 'The Library is Open' (accent bg) when family_safe_mode=true and 'The Library is Closed' (surface bg) when false"
    - "Clicking FSM toggle sends POST /fsm with {enabled: bool} and flips button state optimistically; reverts on error"
    - "New skip SSE events are prepended to the skip feed as li elements with fade-in animation"
    - "A five_skip_warning SSE event removes the hidden attribute from #skip-banner"
    - "Clicking the banner dismiss button sets the hidden attribute on #skip-banner"
    - "docker compose up -d starts both daemon and web_ui services without error"
    - "WEB_UI_PORT is documented in .env.example"
  artifacts:
    - path: "web_ui/templates/index.html"
      provides: "Single-page dashboard HTML with embedded CSS and JS"
      min_lines: 200
      contains: "EventSource('/events')"
    - path: "web_ui/Dockerfile"
      provides: "Container image for FastAPI web_ui service"
      contains: "uvicorn"
    - path: "docker-compose.yml"
      provides: "web_ui service definition alongside daemon"
      contains: "web_ui:"
    - path: ".env.example"
      provides: "WEB_UI_PORT documented"
      contains: "WEB_UI_PORT"
    - path: "Makefile"
      provides: "ui-logs target"
      contains: "ui-logs"
  key_links:
    - from: "web_ui/templates/index.html EventSource('/events')"
      to: "web_ui/main.py GET /events"
      via: "Browser native EventSource API"
      pattern: "EventSource\\('/events'\\)"
    - from: "web_ui/templates/index.html fetch POST /fsm"
      to: "web_ui/main.py POST /fsm"
      via: "fetch() with method: 'POST', Content-Type: application/json"
      pattern: "fetch.*\\/fsm"
    - from: "docker-compose.yml web_ui service"
      to: "web_ui/Dockerfile"
      via: "build context + dockerfile path"
      pattern: "dockerfile.*web_ui"
---

<objective>
Build the HTML/CSS/JS dashboard per UI-SPEC.md and wire the web_ui service into docker-compose.yml. Plan 03-01 delivered web_ui/main.py and the skip event queue. This plan completes the phase by making everything runnable with `docker compose up -d`.

Purpose: The dashboard is the user-visible deliverable for Phase 3. Without HTML and the docker-compose service, nothing from Phase 3 is observable.
Output: web_ui/templates/index.html (full dashboard), web_ui/Dockerfile, updated docker-compose.yml, updated .env.example, updated Makefile.
</objective>

<execution_context>
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/workflows/execute-plan.md
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-signal-notifications-interactive-confirmations/03-CONTEXT.md
@.planning/phases/03-signal-notifications-interactive-confirmations/03-UI-SPEC.md
@.planning/phases/03-signal-notifications-interactive-confirmations/03-01-SUMMARY.md

<interfaces>
<!-- Exact values from UI-SPEC.md that the HTML must implement -->

Color tokens (embed as CSS custom properties in :root):
  --bg: #0d0b08
  --surface: #1c1a14
  --surface-raised: #242018
  --accent: #c9a84c
  --accent-dim: #8a6f2e
  --danger: #a83232
  --warn: #b5722a
  --text: #e8e0cc
  --text-dim: #9a9080
  --text-faint: #5a5448
  --border: #2e2a1e

Fonts: Google Fonts <link> loading Playfair Display (600), Source Sans 3 (400), Courier Prime (400,600)

Typography:
  Heading: 18px / 600 / lh 1.2 / Playfair Display
  Body: 14px / 400 / lh 1.5 / Source Sans 3
  Label/track data: 12-13px / 400-600 / Courier Prime

Spacing:
  Page max-width: 640px centered, horizontal padding: 32px
  Header height: 56px
  Card padding: 16px
  Banner padding: 16px

Required HTML element IDs (exact):
  fsm-toggle — <button> for FSM toggle
  fsm-error — <div> for inline error message
  skip-banner — <div hidden> for five-skip warning
  banner-dismiss — <button aria-label="Dismiss warning"> inside skip-banner
  skip-feed — <ul aria-label="Skip history" aria-live="polite">
  sse-status — <span> in header
  sse-dot — <span> inside sse-status (8px circle)

FSM button states:
  ON: className="fsm-on", background --accent (#c9a84c), color #0d0b08, text "The Library is Open"
  OFF: className="fsm-off", background --surface-raised (#242018), color --text, text "The Library is Closed"
  height: 44px, border-radius: 6px, width: 100%

Skip reason badge classes and styles:
  badge--explicit:  bg rgba(168,50,50,0.2), color #e06060, border rgba(168,50,50,0.3)
  badge--profanity: bg rgba(181,114,42,0.2), color #d4924a, border rgba(181,114,42,0.3)
  badge--adult:     bg rgba(201,168,76,0.15), color #c9a84c, border rgba(201,168,76,0.25)
  badge--approved:  bg rgba(80,140,80,0.15), color #70a870, border rgba(80,140,80,0.25)
  All badges: 11px Courier Prime, padding 4px 8px, border-radius 4px, border 1px solid

Five-skip banner:
  id="skip-banner" hidden initially
  Left border: 4px solid #a83232
  Background: rgba(168,50,50,0.08)
  Padding: 16px

SSE status dot:
  .sse-dot.connected:    background --accent
  .sse-dot.reconnecting: background --warn, plus text "Reconnecting…" at 12px --text-dim

Fade-in animation for new feed items:
  CSS @keyframes fadeIn from opacity:0 to opacity:1, duration 200ms ease-in
  Applied via class "feed-new" on newly prepended li

Exact copywriting strings (use verbatim):
  Page title:          "Family Safe Mode"
  H1 header:           "Spotify Family Safe Mode"
  Log section heading: "Incident Log"
  Empty state heading: "No skips yet"
  Empty state body:    "Skips will appear here in real-time when Family Safe Mode is on."
  Banner copy:         "5 consecutive skips — switch your playlist and resume."
  Banner dismiss:      "✕" (aria-label="Dismiss warning")
  FSM error:           "Could not update — try again."
  SSE reconnecting:    "Reconnecting…"

SSE event format (from daemon.py, Plan 03-01 output):
  Skip:         {"type": "skip", "track": "...", "artist": "...", "reason": "...", "timestamp": "HH:MM:SS"}
  Five-skip:    {"type": "five_skip_warning", "timestamp": "HH:MM:SS"}

Reason-to-badge mapping (JS logic):
  reason.includes("explicit")                      → badge--explicit,  "Flagged: explicit tag"
  reason.includes("profanity") || includes("language") → badge--profanity, "Flagged: strong language"
  reason.includes("adult")                         → badge--adult,     "Flagged: adult themes"
  default (any other skip reason)                  → badge--explicit,  "Flagged: explicit tag"

FSM initial state: HTML contains literal string __FSM_INITIAL__
  web_ui/main.py replaces it with "true" or "false" before serving.
  JS reads: const FSM_INITIAL = "__FSM_INITIAL__"; let fsmEnabled = FSM_INITIAL === "true";

Docker build context: project root (.)
  web_ui/Dockerfile copies full project (COPY . .) so daemon.py is importable
  CMD: uvicorn web_ui.main:app --host 0.0.0.0 --port 8888 --no-access-log
  (port 8888 avoids conflict with Spotify redirect URI at port 8080)

docker-compose.yml existing service (daemon) uses:
  network_mode: host
  env_file: .env
  user: "${UID}:${GID}"
  volumes: state.json, token_cache, lyrics_cache.db
  (web_ui service follows same pattern, adds state.json bind-mount)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard HTML template and web_ui Dockerfile</name>
  <files>web_ui/templates/index.html, web_ui/Dockerfile</files>

  <read_first>
    .planning/phases/03-signal-notifications-interactive-confirmations/03-UI-SPEC.md — authoritative spec; read fully before writing any HTML. All color values, typography specs, component inventories, copywriting, and interaction states are defined here.
    web_ui/main.py — read the dashboard GET / route: it replaces the literal string __FSM_INITIAL__ with "true" or "false". The HTML template MUST contain exactly this placeholder.
    .planning/phases/03-signal-notifications-interactive-confirmations/03-CONTEXT.md — D-03 (no framework, no build step), D-04 (SSE via EventSource), D-07 (newest-first prepend).
  </read_first>

  <action>
Create the directory web_ui/templates/ and write web_ui/templates/index.html.

The file is a single HTML page. Structure:
- <!DOCTYPE html> with lang="en"
- <head>: charset, viewport, <title>Family Safe Mode</title>, Google Fonts preconnect + stylesheet link for Playfair Display:wght@600, Source+Sans+3:wght@400, Courier+Prime:wght@400;600, and a <style> block with ALL CSS
- <body>: page-wrap div containing header, skip-banner, FSM card, skip history card, then a <script> block with ALL JavaScript

CSS rules to implement (embed all in the <style> block):
1. :root — all 11 color custom properties (--bg through --border, exact hex values from interfaces section above)
2. * box-sizing reset, body with background: var(--bg), color: var(--text), font-family Source Sans 3, font-size 14px, line-height 1.5
3. .page-wrap: max-width 640px, margin 0 auto, padding 64px 32px
4. header: display flex, align-items center, justify-content space-between, height 56px, margin-bottom 32px
5. header h1: Playfair Display, 18px, 600, color var(--text)
6. #sse-status: flex, align-items center, gap 6px; .sse-dot: 8px circle, default bg var(--text-faint); .sse-dot.connected bg var(--accent); .sse-dot.reconnecting bg var(--warn)
7. #skip-banner: position relative, border-left 4px solid var(--danger), background rgba(168,50,50,0.08), padding 16px, margin-bottom 24px; #skip-banner[hidden] display:none; #banner-dismiss: position absolute, top 8px, right 8px, background none, border none, color var(--text-dim), cursor pointer
8. .card: background var(--surface), border 1px solid var(--border), border-radius 6px, padding 16px, margin-bottom 24px; .card-heading: Playfair Display 18px 600 lh 1.2 margin-bottom 16px; .section-divider: border-top 1px solid var(--border)
9. #fsm-toggle: width 100%, height 44px, border none, border-radius 6px, cursor pointer, Courier Prime 13px 600; .fsm-on: background var(--accent), color #0d0b08; .fsm-on:hover background var(--accent-dim); .fsm-off: background var(--surface-raised), color var(--text), border 1px solid var(--border); .fsm-off:hover background var(--surface); #fsm-error: margin-top 8px, font-size 12px, color var(--danger), min-height 16px
10. #skip-feed: list-style none, max-height 480px, overflow-y auto; li: display flex, flex-wrap wrap, align-items center, gap 4px, padding 8px 0, border-bottom 1px solid var(--border), Courier Prime 13px, lh 1.4; li.empty: flex-direction column, align-items flex-start; .empty-heading: Playfair Display 14px 600 color var(--text-dim); .empty-body: Source Sans 3 13px color var(--text-faint); .feed-sep: color var(--text-faint); .feed-timestamp: color var(--text-dim) 12px
11. All four badge variants (.badge--explicit, .badge--profanity, .badge--adult, .badge--approved) with exact rgba values, colors, border-colors from interfaces section; base .badge: Courier Prime 11px, padding 4px 8px, border-radius 4px, border 1px solid, line-height 1, white-space nowrap
12. @keyframes fadeIn: from opacity 0 to opacity 1; .feed-new: animation fadeIn 0.2s ease-in

HTML body content:
- <div class="page-wrap">
  - <header><h1>Spotify Family Safe Mode</h1><span id="sse-status"><span class="sse-dot" id="sse-dot"></span><span id="sse-label"></span></span></header>
  - <div id="skip-banner" hidden>5 consecutive skips — switch your playlist and resume.<button id="banner-dismiss" aria-label="Dismiss warning">✕</button></div>
  - <div class="card"><button id="fsm-toggle" class="fsm-off">The Library is Closed</button><div id="fsm-error"></div></div>
  - <div class="card"><h2 class="card-heading">Incident Log</h2><hr class="section-divider"><ul id="skip-feed" aria-label="Skip history" aria-live="polite"><li class="empty"><span class="empty-heading">No skips yet</span><span class="empty-body">Skips will appear here in real-time when Family Safe Mode is on.</span></li></ul></div>
- </div>

JavaScript (embed in <script> before </body>):
1. FSM initial state: const FSM_INITIAL = "__FSM_INITIAL__"; let fsmEnabled = FSM_INITIAL === "true";
2. setFsmUI(enabled): sets fsmEnabled, sets button className to fsm-on or fsm-off, sets textContent to "The Library is Open" or "The Library is Closed"
3. Call setFsmUI(fsmEnabled) on page load
4. fsmBtn click handler (async): newState = !fsmEnabled, call setFsmUI(newState) optimistically, clear fsmError text, then await fetch('/fsm', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({enabled: newState})}); on non-ok response OR catch: call setFsmUI(!newState) to revert, set fsmError.textContent to "Could not update — try again.", setTimeout to clear after 3000ms
5. setBadgeClass(reason): lowercase check — if includes "explicit" return "badge--explicit"; if includes "profanity" or "language" return "badge--profanity"; if includes "adult" return "badge--adult"; default "badge--explicit"
6. badgeLabel(reason): same logic returning "Flagged: explicit tag" / "Flagged: strong language" / "Flagged: adult themes" / "Flagged: explicit tag"
7. removeEmptyState(): remove li.empty from skip-feed if present
8. prependSkipItem(evt): call removeEmptyState(), create li with className "feed-new", append child spans: track text, " — " separator, artist text, " · " separator, badge span with class "badge "+setBadgeClass(evt.reason) and text badgeLabel(evt.reason), " · " separator, timestamp span with class "feed-timestamp"; call skipFeed.insertBefore(li, skipFeed.firstChild)
9. const es = new EventSource('/events'); es.onopen: set sse-dot to "sse-dot connected", clear sse-label text; es.onerror: set sse-dot to "sse-dot reconnecting", set sse-label to "Reconnecting…"; es.onmessage: parse e.data JSON, if type="skip" call prependSkipItem(evt), if type="five_skip_warning" call banner.removeAttribute("hidden")
10. banner-dismiss click: banner.setAttribute("hidden", "")

Write web_ui/Dockerfile:
  FROM python:3.12-slim
  WORKDIR /app
  COPY requirements.txt .
  RUN pip install --no-cache-dir -r requirements.txt
  COPY . .
  CMD ["uvicorn", "web_ui.main:app", "--host", "0.0.0.0", "--port", "8888", "--no-access-log"]

The Dockerfile copies the project root (COPY . .) so both daemon.py and web_ui/ are on PYTHONPATH at /app. Port 8888 avoids conflict with the Spotify redirect URI at 8080.
  </action>

  <verify>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep -c "EventSource('/events')" web_ui/templates/index.html</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "__FSM_INITIAL__" web_ui/templates/index.html</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "id=\"skip-banner\"" web_ui/templates/index.html</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "id=\"fsm-toggle\"" web_ui/templates/index.html</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "aria-live=\"polite\"" web_ui/templates/index.html</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "five_skip_warning" web_ui/templates/index.html</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "The Library is Open\|The Library is Closed" web_ui/templates/index.html</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "Incident Log" web_ui/templates/index.html</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "badge--explicit\|badge--profanity" web_ui/templates/index.html</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "uvicorn" web_ui/Dockerfile</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "8888" web_ui/Dockerfile</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "EventSource('/events')" web_ui/templates/index.html` returns 1
    - `grep "__FSM_INITIAL__" web_ui/templates/index.html` returns 1 line (the JS const assignment)
    - `grep "id=\"skip-banner\"" web_ui/templates/index.html` returns 1 line with "hidden" attribute present on same element
    - `grep "id=\"fsm-toggle\"" web_ui/templates/index.html` returns 1 line
    - `grep 'aria-live="polite"' web_ui/templates/index.html` returns 1 line (the skip-feed ul)
    - `grep "five_skip_warning" web_ui/templates/index.html` returns at least 1 line (the es.onmessage handler)
    - `grep "The Library is Open" web_ui/templates/index.html` returns at least 1 line
    - `grep "Incident Log" web_ui/templates/index.html` returns 1 line
    - `grep "badge--explicit" web_ui/templates/index.html` returns at least 2 lines (CSS def + JS logic)
    - `grep "uvicorn" web_ui/Dockerfile` returns the CMD line
    - `grep "8888" web_ui/Dockerfile` returns 1 line
    - `grep "aria-label=\"Dismiss warning\"" web_ui/templates/index.html` returns 1 line (the ✕ button)
    - `grep "Could not update" web_ui/templates/index.html` returns 1 line (JS fsmError assignment)
    - `grep "c9a84c" web_ui/templates/index.html` returns at least 2 lines (--accent in :root + fsm-on background)
  </acceptance_criteria>

  <done>Dashboard HTML renders the complete UI per UI-SPEC.md: dark theme with color tokens, FSM toggle with two states, skip feed with badges, five-skip banner (hidden by default), SSE status dot, and all copywriting strings verbatim. Dockerfile exists and specifies uvicorn on port 8888.</done>
</task>

<task type="auto">
  <name>Task 2: Wire web_ui service into docker-compose.yml, .env.example, and Makefile</name>
  <files>docker-compose.yml, .env.example, Makefile</files>

  <read_first>
    docker-compose.yml — read fully; must understand existing daemon service definition (network_mode: host, env_file, user, volumes) to replicate correctly for web_ui service
    Makefile — read fully; must understand existing targets (.PHONY list, setup target, auth target) to add ui-logs target without breaking existing targets
    .env.example — read if it exists; add WEB_UI_PORT=8888 without removing existing entries
    .planning/phases/03-signal-notifications-interactive-confirmations/03-CONTEXT.md — D-05 (network_mode: host, shared state.json), D-10 (no auth, LAN only)
  </read_first>

  <action>
**docker-compose.yml — add web_ui service after the daemon service:**

The web_ui service must follow the same patterns as the daemon service:
- `network_mode: host` (D-05: same network as daemon so it can receive events; also required for host filesystem access)
- `env_file: .env` (config from environment)
- `user: "${UID}:${GID}"` (same file ownership pattern as daemon)
- Build from project root with web_ui/Dockerfile
- Bind-mount state.json so POST /fsm can read and write it
- `restart: always` (consistent with daemon service)

Add this service block to docker-compose.yml:
```yaml
  web_ui:
    build:
      context: .
      dockerfile: web_ui/Dockerfile
    restart: always
    network_mode: host
    env_file: .env
    user: "${UID}:${GID}"
    volumes:
      - ./state.json:/app/state.json  # FSM toggle reads/writes this file (D-09)
```

Note: no ports: mapping needed because network_mode: host exposes the uvicorn port (8888) directly on the host.

**Makefile — add ui-logs target and update .PHONY line:**

Add to .PHONY line: `ui-logs`

Add after the existing `logs:` target:
```makefile
ui-logs:
	docker compose logs -f web_ui
```

Also add a `web-ui:` help comment above it following the existing comment style.

**.env.example — add WEB_UI_PORT entry:**

Append the following line to .env.example (after existing entries):
```
WEB_UI_PORT=8888
```

Also add a comment line before it:
```
# Web UI dashboard port (served by FastAPI/uvicorn — Phase 3)
WEB_UI_PORT=8888
```
  </action>

  <verify>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep -n "web_ui:" docker-compose.yml</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep -n "web_ui/Dockerfile" docker-compose.yml</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep -n "network_mode: host" docker-compose.yml</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep -c "network_mode: host" docker-compose.yml</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "state.json" docker-compose.yml</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "ui-logs" Makefile</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && grep "WEB_UI_PORT" .env.example</automated>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && python -c "import yaml; d=yaml.safe_load(open('docker-compose.yml')); print('services:', list(d['services'].keys()))" 2>/dev/null || echo "yaml not installed — check manually"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "web_ui:" docker-compose.yml` returns at least 1 line (service key)
    - `grep "web_ui/Dockerfile" docker-compose.yml` returns 1 line (dockerfile path in build section)
    - `grep -c "network_mode: host" docker-compose.yml` returns 2 (one per service — daemon and web_ui)
    - `grep "state.json" docker-compose.yml` returns 2 lines (one per service bind-mount)
    - `grep "ui-logs" Makefile` returns at least 1 line
    - `grep "ui-logs" Makefile | grep "docker compose logs"` returns 1 line (the target body)
    - `grep "WEB_UI_PORT" .env.example` returns at least 1 line with value 8888
    - `grep "ui-logs" Makefile` also appears in the .PHONY line or a phony declaration
    - The daemon service in docker-compose.yml is unchanged: `grep "restart: always" docker-compose.yml` still returns at least 1 line
  </acceptance_criteria>

  <done>docker-compose.yml has a web_ui service using the project-root build context with web_ui/Dockerfile, network_mode: host, state.json bind-mount, and restart: always. Makefile has ui-logs target. .env.example documents WEB_UI_PORT=8888.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "EventSource('/events')" web_ui/templates/index.html` returns 1 line
2. `grep "__FSM_INITIAL__" web_ui/templates/index.html` returns 1 line
3. `grep "five_skip_warning" web_ui/templates/index.html` returns 1+ lines
4. `grep "web_ui:" docker-compose.yml` returns 1+ lines
5. `grep -c "network_mode: host" docker-compose.yml` returns 2
6. `grep "WEB_UI_PORT" .env.example` returns 1+ lines
7. `grep "ui-logs" Makefile` returns 1+ lines
8. `grep "uvicorn" web_ui/Dockerfile` returns 1 line
9. `python -c "import ast; ast.parse(open('daemon.py').read())"` still exits 0 (from Plan 03-01, unaffected by this plan)
</verification>

<success_criteria>
- Dashboard HTML implements all UI-SPEC.md components: FSM toggle (two states with correct copy), skip feed (SSE-driven, prepend newest), badges (four variants with correct colors), five-skip banner (hidden by default, dismissible), SSE status indicator (connected/reconnecting states)
- All copywriting strings match UI-SPEC.md verbatim (The Library is Open/Closed, Incident Log, No skips yet, 5 consecutive skips copy)
- web_ui service in docker-compose.yml starts with `docker compose up -d` alongside the daemon service
- Makefile ui-logs target lets user tail web_ui container logs
- .env.example documents WEB_UI_PORT
</success_criteria>

<output>
After completion, create `.planning/phases/03-signal-notifications-interactive-confirmations/03-02-SUMMARY.md`
</output>
