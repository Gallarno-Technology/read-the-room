---
phase: 03-signal-notifications-interactive-confirmations
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - web_ui/Dockerfile
  - daemon.py
autonomous: true
gap_closure: true
requirements:
  - FSM-03

must_haves:
  truths:
    - "web_ui container starts successfully and uvicorn serves the dashboard (fastapi and uvicorn are installed from web_ui/requirements.txt)"
    - "consecutive_skips counter resets to 0 when family_safe_mode transitions from False to True within the same poll_loop run"
  artifacts:
    - path: "web_ui/Dockerfile"
      provides: "Correct requirements.txt path — installs fastapi, uvicorn, python-dotenv"
      contains: "web_ui/requirements.txt"
    - path: "daemon.py"
      provides: "FSM transition detection that resets consecutive_skips on False->True toggle"
      contains: "prev_fsm"
  key_links:
    - from: "web_ui/Dockerfile COPY web_ui/requirements.txt"
      to: "pip install"
      via: "RUN pip install --no-cache-dir -r requirements.txt"
      pattern: "web_ui/requirements\\.txt"
    - from: "daemon.py poll_loop prev_fsm check"
      to: "consecutive_skips = 0"
      via: "if not prev_fsm and fsm_now: consecutive_skips = 0"
      pattern: "prev_fsm"
---

<objective>
Fix two targeted bugs identified by the verifier: (1) web_ui/Dockerfile copies the wrong requirements.txt causing the container to exit immediately at startup, and (2) the consecutive_skips counter does not reset when Family Safe Mode is toggled back on.

Purpose: Gap 1 is a blocker — without it no dashboard is served. Gap 3 is a behavioral correctness fix ensuring the 5-skip warning only fires after 5 genuine new skips in the current FSM-on session.
Output: Corrected web_ui/Dockerfile (1 line change), updated daemon.py poll_loop with prev_fsm tracking.
</objective>

<execution_context>
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/workflows/execute-plan.md
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-signal-notifications-interactive-confirmations/03-CONTEXT.md
@.planning/phases/03-signal-notifications-interactive-confirmations/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix web_ui/Dockerfile requirements.txt path (Gap 1)</name>
  <files>web_ui/Dockerfile</files>

  <read_first>
    web_ui/Dockerfile — read the full file (it is 6 lines); line 3 is "COPY requirements.txt ." which copies the project-root requirements.txt; must be changed to copy web_ui/requirements.txt instead
    web_ui/requirements.txt — verify it contains fastapi, uvicorn, python-dotenv (these are the deps the web_ui container actually needs)
  </read_first>

  <action>
Replace line 3 of web_ui/Dockerfile. The current line is:

  COPY requirements.txt .

Change it to:

  COPY web_ui/requirements.txt requirements.txt

The build context for the web_ui service in docker-compose.yml is the project root (context: .), so the COPY source path must include the subdirectory prefix. The destination name "requirements.txt" is kept so the subsequent "RUN pip install --no-cache-dir -r requirements.txt" line (line 4) still works unchanged. The "COPY . ." on line 5 comes after and overlays the full project including web_ui/; it does NOT need to be reordered because the requirements.txt is installed before COPY . . executes.

The complete corrected Dockerfile must be:
  FROM python:3.12-slim
  WORKDIR /app
  COPY web_ui/requirements.txt requirements.txt
  RUN pip install --no-cache-dir -r requirements.txt
  COPY . .
  CMD ["uvicorn", "web_ui.main:app", "--host", "0.0.0.0", "--port", "8888", "--no-access-log"]

Do not change any other line.
  </action>

  <verify>
    <automated>grep -n "requirements.txt" /home/cgallarno/Development/spotify-sentiment/web_ui/Dockerfile</automated>
    <automated>grep "web_ui/requirements.txt" /home/cgallarno/Development/spotify-sentiment/web_ui/Dockerfile</automated>
    <automated>grep "fastapi" /home/cgallarno/Development/spotify-sentiment/web_ui/requirements.txt</automated>
    <automated>grep "uvicorn" /home/cgallarno/Development/spotify-sentiment/web_ui/requirements.txt</automated>
  </verify>

  <acceptance_criteria>
    - `grep "requirements.txt" web_ui/Dockerfile` returns exactly 2 lines: the COPY line and the RUN pip install line
    - `grep "web_ui/requirements.txt" web_ui/Dockerfile` returns 1 line (the COPY line)
    - `grep "COPY requirements.txt \." web_ui/Dockerfile` returns 0 lines (the wrong path is gone)
    - `grep "fastapi" web_ui/requirements.txt` returns the "fastapi==0.115.12" line
    - `grep "uvicorn" web_ui/requirements.txt` returns the "uvicorn[standard]==0.34.0" line
    - The CMD line in web_ui/Dockerfile still contains "uvicorn web_ui.main:app" and port 8888
  </acceptance_criteria>

  <done>web_ui/Dockerfile installs from web_ui/requirements.txt so the container has fastapi and uvicorn; the container no longer exits at startup due to a missing module.</done>
</task>

<task type="auto">
  <name>Task 2: Reset consecutive_skips on FSM False→True transition (Gap 3)</name>
  <files>daemon.py</files>

  <read_first>
    daemon.py — read lines 85–200 (poll_loop function); the consecutive_skips variable is initialized at line 97; family_safe_mode is read via state.get("family_safe_mode", False) inside the while loop at line 136; there is currently no tracking of the previous FSM value
  </read_first>

  <action>
Make two targeted additions to daemon.py poll_loop. Do NOT restructure or reformat existing code.

**Addition 1 — Initialize prev_fsm tracker alongside consecutive_skips.**

Current lines 97–98 of daemon.py:
  consecutive_skips: int = 0
  last_heartbeat = time.monotonic()

Change to add prev_fsm initialization on the line immediately after consecutive_skips:
  consecutive_skips: int = 0
  prev_fsm: bool = False
  last_heartbeat = time.monotonic()

**Addition 2 — Detect FSM False→True transition and reset counter.**

The FSM value is read at the top of the track-change block:
  if state.get("family_safe_mode", False):

Add a FSM transition check immediately BEFORE this if-block (i.e., after the save_state + reload block at lines 130–132, before line 134 "# Phase 2: Content filtering"). The new lines:

  # Gap-3 fix: reset consecutive_skips when FSM transitions from off to on
  fsm_now = state.get("family_safe_mode", False)
  if not prev_fsm and fsm_now:
      consecutive_skips = 0
      log.info("[FSM] consecutive_skips reset — FSM re-enabled")
  prev_fsm = fsm_now

This must be placed after the `state = load_state()` reload (line 131) so it reads the freshest FSM value, and before `if state.get("family_safe_mode", False):` so the counter is already reset before any skip logic runs for this poll cycle.

No other changes to daemon.py.
  </action>

  <verify>
    <automated>python -c "import ast; ast.parse(open('/home/cgallarno/Development/spotify-sentiment/daemon.py').read()); print('syntax OK')"</automated>
    <automated>grep -n "prev_fsm" /home/cgallarno/Development/spotify-sentiment/daemon.py</automated>
    <automated>grep -n "consecutive_skips = 0" /home/cgallarno/Development/spotify-sentiment/daemon.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('daemon.py').read())"` exits 0 with no SyntaxError
    - `grep "prev_fsm" daemon.py` returns at least 3 lines: initialization (prev_fsm: bool = False), the comparison (if not prev_fsm and fsm_now:), and the assignment (prev_fsm = fsm_now)
    - `grep -c "consecutive_skips = 0" daemon.py` returns at least 3 lines: initialization (line 97), reset on FSM re-enable (new addition), and reset after 5-skip trigger (line 188)
    - `grep "fsm_now" daemon.py` returns at least 2 lines: the assignment and the comparison
    - The existing "consecutive_skips = 0" at line ~153 (reset on allow) is unchanged
    - The existing "consecutive_skips = 0" at line ~188 (reset after 5-skip trigger) is unchanged
  </acceptance_criteria>

  <done>daemon.py poll_loop tracks prev_fsm and resets consecutive_skips to 0 whenever family_safe_mode transitions from False to True, preventing premature 5-skip warnings after FSM is re-enabled mid-session.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "web_ui/requirements.txt" web_ui/Dockerfile` returns 1 line
2. `grep "COPY requirements.txt \." web_ui/Dockerfile` returns 0 lines (wrong path removed)
3. `python -c "import ast; ast.parse(open('daemon.py').read())"` exits 0
4. `grep -c "prev_fsm" daemon.py` returns at least 3
5. `grep -c "consecutive_skips = 0" daemon.py` returns at least 3
</verification>

<success_criteria>
- web_ui/Dockerfile line 3 is "COPY web_ui/requirements.txt requirements.txt" — container installs fastapi and uvicorn on build
- daemon.py poll_loop initializes prev_fsm: bool = False, updates it each cycle, and resets consecutive_skips when FSM transitions False→True
- All existing daemon.py behavior is preserved (no other lines changed)
</success_criteria>

<output>
After completion, create `.planning/phases/03-signal-notifications-interactive-confirmations/03-03-SUMMARY.md`
</output>
