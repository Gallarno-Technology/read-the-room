---
phase: 03-signal-notifications-interactive-confirmations
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - skip_client.py
  - daemon.py
autonomous: true
gap_closure: true
requirements: [SIG-04]

must_haves:
  truths:
    - "After 5 consecutive explicit tracks, Spotify playback pauses"
    - "Pause works on Sonos (is_restricted=True) via SoCo speaker.pause()"
    - "Pause works on non-Sonos devices via Spotify API pause_playback(device_id)"
    - "Pause failure logs an error but does not crash the daemon"
  artifacts:
    - path: "skip_client.py"
      provides: "pause() method on SkipClient ABC and both implementations"
      exports: ["SkipClient.pause", "SocoSkipClient.pause", "SpotifySkipClient.pause"]
    - path: "daemon.py"
      provides: "5-skip trigger calls await client.pause() instead of sp.pause_playback()"
      contains: "await client.pause"
  key_links:
    - from: "daemon.py"
      to: "skip_client.py"
      via: "await client.pause(device_name, device.get('id'))"
      pattern: "await client\\.pause"
    - from: "SocoSkipClient.pause"
      to: "soco.SoCo.pause"
      via: "run_in_executor"
      pattern: "speaker\\.pause"
    - from: "SpotifySkipClient.pause"
      to: "sp.pause_playback"
      via: "run_in_executor with device_id"
      pattern: "pause_playback"
---

<objective>
Fix the 5-skip pause feature so playback actually stops after 5 consecutive explicit tracks on Sonos devices.

Purpose: The UAT revealed sp.pause_playback() is called synchronously and without a device_id, and it silently fails for Sonos (is_restricted=True) because the Spotify API cannot control restricted devices. The pause must go through the same SoCo path used for skips.

Output: pause() method added to SkipClient ABC and both implementations; daemon.py 5-skip trigger updated to await the correct client's pause().
</objective>

<execution_context>
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/workflows/execute-plan.md
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/cgallarno/Development/spotify-sentiment/.planning/PROJECT.md
@/home/cgallarno/Development/spotify-sentiment/.planning/ROADMAP.md
@/home/cgallarno/Development/spotify-sentiment/.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pause() to SkipClient ABC and both implementations</name>
  <files>skip_client.py</files>
  <behavior>
    - Test: SpotifySkipClient.pause(device_name, device_id) calls sp.pause_playback(device_id) via run_in_executor and returns True on success
    - Test: SpotifySkipClient.pause returns False and logs error on SpotifyException
    - Test: SocoSkipClient.pause(device_name, device_id) calls speaker.pause() via run_in_executor using cached IP if available, returns True
    - Test: SocoSkipClient.pause falls back to discovery when IP not cached, returns True
    - Test: SocoSkipClient.pause returns False and logs warning if speaker not found on network
    - Test: SkipClient ABC exposes pause() as an abstractmethod
  </behavior>
  <action>
Add `pause(device_name: str, device_id: str) -> bool` as an abstractmethod to the `SkipClient` ABC (same signature as skip()).

Implement `SpotifySkipClient.pause`:
- Use `loop.run_in_executor(None, self.sp.pause_playback, device_id)` — pass device_id so the API targets the correct device (bare sp.pause_playback() with no device_id fails for non-active sessions)
- Catch `SpotifyException`, log error, return False
- Return True on success

Implement `SocoSkipClient.pause`:
- Mirror the exact structure of `SocoSkipClient.skip()` — use the same IP cache + SSDP discovery fallback pattern
- Replace `speaker.next` / `device.next` with `speaker.pause` / `device.pause`
- All SoCo calls go through `run_in_executor` (same as skip)
- Return True/False with same log messages as skip but with "pause" in the text
- Catch `SoCoUPnPException`, log error, return False
  </action>
  <verify>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && python -m pytest tests/test_skip_client.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>SkipClient.pause is abstract; SocoSkipClient.pause and SpotifySkipClient.pause pass all tests; no existing skip tests broken</done>
</task>

<task type="auto">
  <name>Task 2: Replace sp.pause_playback() in daemon.py with await client.pause()</name>
  <files>daemon.py</files>
  <action>
In `poll_loop`, find the 5-skip block (around line 204-217). Replace:

```python
try:
    sp.pause_playback()
except Exception as exc:  # noqa: BLE001
    log.error("[5SKIP] pause_playback failed: %s", exc)
```

With:

```python
paused = await client.pause(device_name, device.get("id"))
if not paused:
    log.warning("[5SKIP] pause failed for device %r — playback may continue", device_name)
```

The variable `client` is already in scope (assigned two lines above for the skip call: `client = soco_skip if is_restricted else spotify_skip`). No new variable needed.

Do NOT modify any other part of daemon.py. The five_skip_warning events and queue appends that follow the pause call stay exactly as they are.
  </action>
  <verify>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && python -c "import ast, sys; ast.parse(open('daemon.py').read()); print('syntax ok')" && grep -n "await client.pause" daemon.py</automated>
  </verify>
  <done>daemon.py parses without error; `await client.pause` appears in the 5-skip block; `sp.pause_playback()` no longer appears in daemon.py</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_skip_client.py -x -q` — all pause tests pass
2. `grep -n "sp\.pause_playback" daemon.py` returns no results
3. `grep -n "await client\.pause" daemon.py` returns exactly one match in the 5-skip block
4. `python -c "import ast; ast.parse(open('daemon.py').read())"` succeeds
5. `python -c "import ast; ast.parse(open('skip_client.py').read())"` succeeds
6. Existing skip tests still pass: `python -m pytest tests/test_skip_client.py -q`
</verification>

<success_criteria>
- SocoSkipClient has a pause() method that calls speaker.pause() via run_in_executor
- SpotifySkipClient has a pause() method that calls sp.pause_playback(device_id) via run_in_executor
- SkipClient ABC declares pause() as abstractmethod
- daemon.py uses `await client.pause(device_name, device.get("id"))` in the 5-skip block
- sp.pause_playback() no longer called anywhere in daemon.py
- All existing tests pass
</success_criteria>

<output>
After completion, create `/home/cgallarno/Development/spotify-sentiment/.planning/phases/03-signal-notifications-interactive-confirmations/03-05-SUMMARY.md`
</output>
