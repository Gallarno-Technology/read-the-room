---
phase: 30-per-user-daemon-management
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_web_ui_endpoints.py
  - tests/test_daemon_events.py
  - tests/test_manage_users.py
autonomous: true
requirements:
  - PROC-01
  - PROC-02
  - PROC-03
  - PROC-04

must_haves:
  truths:
    - "All Phase 30 behaviors have failing tests before any implementation touches production code"
    - "Test suite exits non-zero when run against the current (unmodified) codebase"
    - "Each test is independently runnable with -k filter for CI granularity"
  artifacts:
    - path: "tests/test_web_ui_endpoints.py"
      provides: "Supervisor, _spawn_daemon, lifespan, OAuth callback test cases"
      contains: "test_spawn_daemon_env_vars, test_supervisor_restarts_on_crash, test_lifespan_boots_active_users"
    - path: "tests/test_daemon_events.py"
      provides: "Consecutive 401 counter test cases"
      contains: "test_three_consecutive_401s_trigger_exit2, test_401_counter_resets_on_success"
    - path: "tests/test_manage_users.py"
      provides: "SIGTERM/SIGKILL daemon stop test cases"
      contains: "test_remove_sends_sigterm_to_daemon, test_remove_handles_missing_pid_file"
  key_links:
    - from: "tests/test_web_ui_endpoints.py"
      to: "web_ui/main.py"
      via: "import web_ui.main as web_ui_main"
      pattern: "_spawn_daemon|_supervisor_for_uid|lifespan"
    - from: "tests/test_daemon_events.py"
      to: "daemon.py"
      via: "poll_loop() consecutive 401 path"
      pattern: "consecutive_401|sys.exit.*2"
    - from: "tests/test_manage_users.py"
      to: "scripts/manage_users.py"
      via: "_stop_daemon_via_pid"
      pattern: "_stop_daemon_via_pid|SIGTERM|SIGKILL"
---

<objective>
Write all Wave 0 failing test scaffolds for Phase 30 before any production code changes.

Purpose: Enforce TDD — tests define the contract that implementation must satisfy. Running the suite against the unmodified codebase must fail for all new test cases.
Output: Failing tests in three test files; existing passing tests remain green.
</objective>

<execution_context>
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/workflows/execute-plan.md
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-per-user-daemon-management/30-CONTEXT.md
@.planning/phases/30-per-user-daemon-management/30-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for _spawn_daemon, _supervisor_for_uid, and lifespan in test_web_ui_endpoints.py</name>
  <files>tests/test_web_ui_endpoints.py</files>

  <read_first>
    - tests/test_web_ui_endpoints.py — read in full before adding any tests; understand existing mock patterns, fixtures, import style, and the test_callback_spawns_daemon test that will need updating
    - web_ui/main.py — lines 1-60 (imports and app definition), lines 152-165 (_tails/_subscribers dicts), lines 475-506 (existing fire-and-forget spawn in /auth/callback)
    - .planning/phases/30-per-user-daemon-management/30-CONTEXT.md — D-04 through D-13 for exact supervisor/spawn contracts
    - .planning/phases/30-per-user-daemon-management/30-RESEARCH.md — Architecture Patterns section for _spawn_daemon, _supervisor_for_uid, and lifespan code shapes
  </read_first>

  <behavior>
    New test cases to add (all must FAIL against current codebase):

    **_spawn_daemon env vars (PROC-01, PROC-03):**
    - test_spawn_daemon_sets_env_vars: Call `await web_ui_main._spawn_daemon(uid)` with mocked `asyncio.create_subprocess_exec`. Assert the `env` kwarg contains `STATE_PATH`, `EVENTS_PATH`, `LYRICS_DB_PATH`, `SPOTIFY_CACHE_PATH`, and `POLL_INTERVAL_SECONDS="3"`. (Fails: `_spawn_daemon` does not yet exist.)

    **_spawn_daemon PID file (D-11):**
    - test_spawn_daemon_writes_pid_file: Call `await web_ui_main._spawn_daemon(uid)` with mocked subprocess whose `.pid = 12345`. Assert that `users/{uid}/daemon.pid` contains `"12345"`. (Fails: `_spawn_daemon` does not yet exist.)

    **_spawn_daemon stores in _daemons (D-05):**
    - test_spawn_daemon_stores_in_daemons_dict: Call `await web_ui_main._spawn_daemon(uid)` with mock. Assert `web_ui_main._daemons[uid]` is set to the mock process. (Fails: `_daemons` dict and `_spawn_daemon` do not yet exist.)

    **Supervisor restart on unexpected exit (PROC-02, D-07):**
    - test_supervisor_restarts_on_unexpected_exit: Prime `_daemons[uid]` with a mock process whose `wait()` returns exit code `1` first, then `0` on the re-spawn's process. Call `await web_ui_main._supervisor_for_uid(uid)` and assert `_spawn_daemon` was called twice. (Fails: `_supervisor_for_uid` does not yet exist.)

    **Supervisor does NOT restart on exit code 0 (D-07):**
    - test_supervisor_does_not_restart_on_clean_exit: Prime with exit code `0`. Assert `_spawn_daemon` called exactly once (the initial prime, not by supervisor). (Fails: `_supervisor_for_uid` does not yet exist.)

    **Supervisor does NOT restart on exit code 2 (D-03):**
    - test_supervisor_does_not_restart_on_token_revoked: Prime with exit code `2`. Assert `_spawn_daemon` not called by supervisor. (Fails: `_supervisor_for_uid` does not yet exist.)

    **Supervisor exits when uid removed (D-13):**
    - test_supervisor_exits_when_uid_removed: Prime with exit code `1` (unexpected), but mock `_registry.load()` to return empty list (uid gone). Assert supervisor exits the while-loop without calling `_spawn_daemon` again. (Fails: `_supervisor_for_uid` does not yet exist.)

    **Lifespan boots all active users (PROC-04, D-09):**
    - test_lifespan_spawns_daemons_for_active_users: Use TestClient context manager (which triggers lifespan). Mock `_registry.load()` to return two active user records. Mock `_spawn_daemon` and `asyncio.create_task`. Assert `_spawn_daemon` called twice with each uid. Assert `asyncio.create_task` called twice (supervisor tasks). (Fails: lifespan not yet added to app.)

    **Lifespan skips pending users (PROC-04):**
    - test_lifespan_skips_pending_users: Mock `_registry.load()` to return one `status="pending"` user. Assert `_spawn_daemon` not called. (Fails: lifespan not yet added.)

    **OAuth callback creates supervisor task (RESEARCH.md Pitfall 2):**
    - test_callback_also_creates_supervisor_task: Update the existing `test_callback_spawns_daemon` test to additionally assert that `asyncio.create_task` was called with a `_supervisor_for_uid` coroutine. (Fails: callback calls bare `asyncio.create_subprocess_exec` without supervisor task.)
  </behavior>

  <action>
    Append the new test functions to tests/test_web_ui_endpoints.py. Do NOT modify existing passing tests — only add new ones and update test_callback_spawns_daemon.

    Required imports to add at the top of the file (only if not already present):
    - `import asyncio` (check if already imported as `_asyncio` or `asyncio`)
    - No new packages — all mocking uses `unittest.mock`

    Pattern to follow for async tests: the existing test file uses `TestClient` (synchronous Starlette test client that drives the asyncio event loop). For testing pure async coroutines like `_supervisor_for_uid` directly, use `asyncio.run()` or `pytest.mark.anyio` if anyio is available. Check the existing conftest.py for available fixtures.

    For `test_lifespan_spawns_daemons_for_active_users`, use `TestClient(web_ui_main.app)` as a context manager — entering the context triggers the lifespan startup.

    Helper fixture for active user record (add near existing `_pending_user_record` helper):
    ```python
    def _active_user_record(uid: str, name: str = "Test User") -> dict:
        return {"uid": uid, "name": name, "status": "active", "created_at": "2026-01-01T00:00:00Z"}
    ```

    For supervisor coroutine tests that call `await web_ui_main._supervisor_for_uid(uid)` directly, use `asyncio.run()` to drive them from a synchronous test function. These tests will need the module-level `_daemons` dict to be primed and the mock process returned by `_spawn_daemon` to eventually return exit code 0 to terminate the while-loop.

    Expected test outcomes when run against UNMODIFIED codebase:
    - All new tests: FAIL (AttributeError on `_spawn_daemon`, `_supervisor_for_uid`, `_daemons`)
    - All existing tests: PASS (unchanged)
  </action>

  <verify>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && uv run pytest tests/test_web_ui_endpoints.py -x -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_web_ui_endpoints.py contains `def test_spawn_daemon_sets_env_vars(`
    - tests/test_web_ui_endpoints.py contains `def test_spawn_daemon_writes_pid_file(`
    - tests/test_web_ui_endpoints.py contains `def test_spawn_daemon_stores_in_daemons_dict(`
    - tests/test_web_ui_endpoints.py contains `def test_supervisor_restarts_on_unexpected_exit(`
    - tests/test_web_ui_endpoints.py contains `def test_supervisor_does_not_restart_on_clean_exit(`
    - tests/test_web_ui_endpoints.py contains `def test_supervisor_does_not_restart_on_token_revoked(`
    - tests/test_web_ui_endpoints.py contains `def test_supervisor_exits_when_uid_removed(`
    - tests/test_web_ui_endpoints.py contains `def test_lifespan_spawns_daemons_for_active_users(`
    - tests/test_web_ui_endpoints.py contains `def test_lifespan_skips_pending_users(`
    - tests/test_web_ui_endpoints.py contains `def test_callback_also_creates_supervisor_task(` OR the existing `test_callback_spawns_daemon` asserts `asyncio.create_task` was called
    - Running `uv run pytest tests/test_web_ui_endpoints.py -k "spawn_daemon or supervisor or lifespan" -q` exits non-zero (new tests fail)
    - Running `uv run pytest tests/test_web_ui_endpoints.py -k "not spawn_daemon and not supervisor and not lifespan" -q` exits zero (existing tests still pass)
  </acceptance_criteria>

  <done>Failing test scaffolds for _spawn_daemon, _supervisor_for_uid, and lifespan committed; existing tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests for daemon.py 401 counter and manage_users.py PID kill</name>
  <files>tests/test_daemon_events.py, tests/test_manage_users.py</files>

  <read_first>
    - tests/test_daemon_events.py — read in full; understand how poll_loop is driven (mock Spotify client, asyncio event fixtures), existing SpotifyException mock pattern
    - tests/test_manage_users.py — read in full; understand patched_registry fixture, how cmd_remove is called, existing remove test
    - daemon.py lines 560-601 — existing 401 error path at line 590-592; success path location for counter reset
    - scripts/manage_users.py cmd_remove function — lines 91-105, current structure before PID kill is added
    - .planning/phases/30-per-user-daemon-management/30-CONTEXT.md — D-01, D-02, D-12 for exact exit code and PID kill contracts
  </read_first>

  <behavior>
    **daemon.py tests (D-01, D-02 — add to test_daemon_events.py):**

    - test_three_consecutive_401s_trigger_exit2: Drive `poll_loop()` with a mock Spotify client whose `current_playback()` raises `SpotifyException(http_status=401)` on three consecutive calls. Assert `sys.exit` is called with argument `2`. Use `pytest.raises(SystemExit)` and check `excinfo.value.code == 2`. (Fails: no counter in current 401 handler.)

    - test_consecutive_401_counter_resets_on_success: Drive `poll_loop()` with mock Spotify client: two calls raise `SpotifyException(401)`, third call succeeds (returns normal playback dict), fourth call raises `SpotifyException(401)`. Assert `sys.exit` is NOT called after the fourth 401 (counter reset to 0 on success, so only 1 consecutive at that point). (Fails: no counter reset logic exists.)

    - test_single_401_does_not_exit: Drive `poll_loop()` with one `SpotifyException(401)` then a clean exit signal. Assert `sys.exit(2)` not called. (Fails: would pass currently, but scaffold documents the contract.)

    **manage_users.py tests (D-12 — add to test_manage_users.py):**

    - test_remove_sends_sigterm_to_daemon: Create a tmp users/{uid}/daemon.pid file containing a fake PID. Mock `os.kill` and `os.kill(pid, 0)` to raise `ProcessLookupError` (simulating process died after SIGTERM). Call `cmd_remove(uid)` with patched registry. Assert `os.kill` was called with `(fake_pid, signal.SIGTERM)`. (Fails: `_stop_daemon_via_pid` does not exist in manage_users.py.)

    - test_remove_sigkills_if_process_survives: Create PID file. Mock `os.kill(pid, 0)` to always succeed (process never dies). Mock `time.sleep` to avoid real sleeping. Call `cmd_remove(uid)`. Assert `os.kill` was eventually called with `(fake_pid, signal.SIGKILL)`. (Fails: no kill logic yet.)

    - test_remove_handles_missing_pid_file: Create user dir without daemon.pid. Call `cmd_remove(uid)`. Assert it succeeds (returns 0) without raising any exception. (Fails: `_stop_daemon_via_pid` called with FileNotFoundError handling does not exist yet.)

    - test_remove_handles_already_dead_process: Create PID file with a fake PID. Mock `os.kill(pid, signal.SIGTERM)` to raise `ProcessLookupError`. Call `cmd_remove(uid)`. Assert it succeeds (returns 0). (Fails: `_stop_daemon_via_pid` does not exist yet.)
  </behavior>

  <action>
    **For test_daemon_events.py:**

    Study how the existing tests drive `poll_loop()`. The function signature is `async def poll_loop(sp, stop_event, ...)`. Existing tests use `asyncio.run()` with a pre-set stop_event or by mocking `asyncio.sleep` to advance time. For the 401 counter tests, mock `sp.current_playback()` to return `SpotifyException(http_status=401, reason="Unauthorized", msg="")` using `side_effect`.

    Import additions needed:
    ```python
    from spotipy.exceptions import SpotifyException
    ```
    (Check if already imported in the test file. If `SpotifyException` is already imported via another path, use that.)

    Use `with pytest.raises(SystemExit) as excinfo:` to catch `sys.exit(2)`. After the context, assert `excinfo.value.code == 2`.

    For the counter reset test, the challenge is that `poll_loop()` runs forever — use a stop_event that gets set after N iterations, or mock `asyncio.sleep` to count calls and set stop_event after the test sequence completes.

    **For test_manage_users.py:**

    Add a new fixture or inline the PID file setup using `tmp_path`. Since `patched_registry` already redirects UserRegistry to `tmp_path`, use the same `tmp_path` to create `tmp_path / "users" / uid / "daemon.pid"`.

    Import additions to add at top of test_manage_users.py (only if not present):
    ```python
    import os
    import signal
    from unittest.mock import call, patch
    ```

    Test pattern for `test_remove_sends_sigterm_to_daemon`:
    ```python
    def test_remove_sends_sigterm_to_daemon(tmp_path, patched_registry):
        uid = "testuid001"
        registry = UserRegistry(base_dir=str(tmp_path))
        registry.provision("Test User")  # creates users/{uid}/ directory
        # We need to use the actual uid from provision OR create it manually
        pid_dir = tmp_path / "users" / uid
        pid_dir.mkdir(parents=True, exist_ok=True)
        (pid_dir / "daemon.pid").write_text("99999")
        with patch("scripts.manage_users.os.kill") as mock_kill, \
             patch("scripts.manage_users.os.kill", side_effect=lambda pid, sig: (_ for _ in ()).throw(ProcessLookupError) if sig == 0 else None) as mock_kill:
            # Alternative: use a simpler approach with side_effect list
            ...
    ```

    Simplest approach: import `_stop_daemon_via_pid` directly from manage_users and test it in isolation. This avoids the complexity of wiring the full `cmd_remove` call with patched registry.

    ```python
    from scripts.manage_users import _stop_daemon_via_pid
    
    def test_remove_sends_sigterm_to_daemon(tmp_path):
        uid = "testuid001"
        pid_dir = tmp_path / "users" / uid
        pid_dir.mkdir(parents=True)
        (pid_dir / "daemon.pid").write_text("99999")
        sigterm_calls = []
        def fake_kill(pid, sig):
            sigterm_calls.append((pid, sig))
            if sig == 0:
                raise ProcessLookupError()  # process is dead after SIGTERM
        with patch("os.kill", side_effect=fake_kill):
            _stop_daemon_via_pid(uid, str(tmp_path))
        assert (99999, signal.SIGTERM) in sigterm_calls
    ```

    These tests will fail with ImportError (`_stop_daemon_via_pid` not defined yet) — which is the correct RED state.
  </action>

  <verify>
    <automated>cd /home/cgallarno/Development/spotify-sentiment && uv run pytest tests/test_daemon_events.py tests/test_manage_users.py -k "consecutive_401 or remove_daemon or sigterm or sigkill or missing_pid or already_dead" -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_daemon_events.py contains `def test_three_consecutive_401s_trigger_exit2(`
    - tests/test_daemon_events.py contains `def test_consecutive_401_counter_resets_on_success(`
    - tests/test_manage_users.py contains `def test_remove_sends_sigterm_to_daemon(`
    - tests/test_manage_users.py contains `def test_remove_sigkills_if_process_survives(`
    - tests/test_manage_users.py contains `def test_remove_handles_missing_pid_file(`
    - tests/test_manage_users.py contains `def test_remove_handles_already_dead_process(`
    - Running `uv run pytest tests/test_daemon_events.py -k "consecutive_401" -q` exits non-zero (tests fail — counter not implemented)
    - Running `uv run pytest tests/test_manage_users.py -k "sigterm or sigkill or missing_pid or already_dead" -q` exits non-zero (tests fail — _stop_daemon_via_pid not yet defined)
    - Running `uv run pytest tests/test_daemon_events.py tests/test_manage_users.py -k "not consecutive_401 and not sigterm and not sigkill and not missing_pid and not already_dead" -q` exits zero (existing tests still pass)
  </acceptance_criteria>

  <done>Failing test scaffolds for daemon.py 401 counter and manage_users.py PID kill committed; existing tests remain green.</done>
</task>

</tasks>

<verification>
Run full suite to confirm all pre-existing tests remain green:
```
uv run pytest tests/ -x -q --ignore=tests/test_web_ui_endpoints.py -k "not consecutive_401 and not sigterm and not sigkill and not missing_pid and not already_dead"
```

Confirm new tests fail (not yet implemented):
```
uv run pytest tests/ -k "spawn_daemon or supervisor or lifespan or consecutive_401 or sigterm or sigkill" -q
```
Expected: failures only on new test functions; non-zero exit code from new tests.
</verification>

<success_criteria>
- All new test functions added to the three test files
- New tests fail with AttributeError or ImportError against unmodified codebase (correct RED state)
- All pre-existing tests remain passing
- `uv run pytest tests/ -q` shows failures only in newly added tests
</success_criteria>

<output>
After completion, create `.planning/phases/30-per-user-daemon-management/30-01-SUMMARY.md`
</output>
