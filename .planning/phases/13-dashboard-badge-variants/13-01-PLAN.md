---
phase: 13-dashboard-badge-variants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web_ui/templates/index.html
autonomous: false
requirements:
  - UI-01

must_haves:
  truths:
    - "A skip caused by drug reference shows a purple 'Drug reference' badge in the skip feed"
    - "A skip caused by sexual content shows a pink 'Sexual content' badge in the skip feed"
    - "Existing badges (explicit, profanity, adult) render identically — no visual regression"
    - "Dashboard loads without JS errors on pre-v1.3 skip entries lacking drug_reference/sexual_content fields"
  artifacts:
    - path: "web_ui/templates/index.html"
      provides: "CSS badge classes badge--drug-reference and badge--sexual-content"
      contains: "badge--drug-reference"
    - path: "web_ui/templates/index.html"
      provides: "JS detection branches in setBadgeClass and badgeLabel"
      contains: "r.includes('drug')"
  key_links:
    - from: "daemon.py result.reason"
      to: "setBadgeClass(evt.reason)"
      via: "SSE skip event evt.reason field; 'drug_reference' contains 'drug', 'sexual_content' contains 'sexual'"
      pattern: "r\\.includes\\('drug'\\)"
    - from: "setBadgeClass return value"
      to: "badge element className in prependSkipItem"
      via: "'badge ' + setBadgeClass(evt.reason)"
      pattern: "badge--drug-reference"
---

<objective>
Add CSS classes and JS detection branches so skip feed entries for drug-reference and sexual-content skips display visually distinct badges instead of the generic fallback.

Purpose: Completes the v1.3 dashboard surface — operators can immediately see which content signal triggered a skip.
Output: Two new CSS badge classes and two new JS branches in web_ui/templates/index.html.
</objective>

<execution_context>
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/workflows/execute-plan.md
@/home/cgallarno/Development/spotify-sentiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-dashboard-badge-variants/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add badge--drug-reference and badge--sexual-content CSS classes</name>
  <files>web_ui/templates/index.html</files>
  <read_first>
    web_ui/templates/index.html — read lines 307-312 (the .badge--fsm-off block, which is the last badge class before @keyframes) to identify the exact insertion point
  </read_first>
  <action>
Insert two new CSS classes immediately after the .badge--fsm-off block (after line 311, before the blank line and @keyframes at line 313).

Add these exact rules:

    .badge--drug-reference {
      background: rgba(130, 80, 190, 0.2);
      color: #a878d4;
      border-color: rgba(130, 80, 190, 0.3);
    }

    .badge--sexual-content {
      background: rgba(190, 80, 140, 0.2);
      color: #d478a8;
      border-color: rgba(190, 80, 140, 0.3);
    }

The insertion point is after the closing brace of .badge--fsm-off and before the blank line that precedes @keyframes fadeIn. No other CSS is changed.
  </action>
  <verify>
    grep -n "badge--drug-reference\|badge--sexual-content" web_ui/templates/index.html
    — must show 4 matches: one selector line each plus the color values embedded in the blocks
    grep -c "badge--drug-reference" web_ui/templates/index.html → must be >= 1
    grep -c "badge--sexual-content" web_ui/templates/index.html → must be >= 1
    grep "rgba(130, 80, 190" web_ui/templates/index.html → must match
    grep "rgba(190, 80, 140" web_ui/templates/index.html → must match
    grep "#a878d4" web_ui/templates/index.html → must match
    grep "#d478a8" web_ui/templates/index.html → must match
  </verify>
  <acceptance_criteria>
    - web_ui/templates/index.html contains `.badge--drug-reference {`
    - web_ui/templates/index.html contains `background: rgba(130, 80, 190, 0.2);`
    - web_ui/templates/index.html contains `color: #a878d4;`
    - web_ui/templates/index.html contains `border-color: rgba(130, 80, 190, 0.3);`
    - web_ui/templates/index.html contains `.badge--sexual-content {`
    - web_ui/templates/index.html contains `background: rgba(190, 80, 140, 0.2);`
    - web_ui/templates/index.html contains `color: #d478a8;`
    - web_ui/templates/index.html contains `border-color: rgba(190, 80, 140, 0.3);`
    - Existing .badge--fsm-off block is unchanged (grep for `rgba(90, 84, 72, 0.15)` still matches)
    - New classes appear before @keyframes fadeIn in document order
  </acceptance_criteria>
  <done>Two new badge CSS classes present with exact purple and pink/magenta color values from D-04 and D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Extend setBadgeClass and badgeLabel with drug and sexual branches</name>
  <files>web_ui/templates/index.html</files>
  <read_first>
    web_ui/templates/index.html — read lines 440-455 (the setBadgeClass and badgeLabel function bodies) to verify current branch order before editing
  </read_first>
  <action>
In setBadgeClass (currently lines ~440-446), insert two new branches between the profanity check and the adult check.

Current function body:
    function setBadgeClass(reason) {
      const r = (reason || '').toLowerCase();
      if (r.includes('explicit')) return 'badge--explicit';
      if (r.includes('profanity') || r.includes('language')) return 'badge--profanity';
      if (r.includes('adult')) return 'badge--adult';
      return 'badge--explicit';
    }

Replace with:
    function setBadgeClass(reason) {
      const r = (reason || '').toLowerCase();
      if (r.includes('explicit')) return 'badge--explicit';
      if (r.includes('profanity') || r.includes('language')) return 'badge--profanity';
      if (r.includes('drug')) return 'badge--drug-reference';
      if (r.includes('sexual')) return 'badge--sexual-content';
      if (r.includes('adult')) return 'badge--adult';
      return 'badge--explicit';
    }

In badgeLabel (currently lines ~448-454), insert matching branches between the profanity check and the adult check.

Current function body:
    function badgeLabel(reason) {
      const r = (reason || '').toLowerCase();
      if (r.includes('explicit')) return 'Flagged: explicit tag';
      if (r.includes('profanity') || r.includes('language')) return 'Flagged: strong language';
      if (r.includes('adult')) return 'Flagged: adult themes';
      return 'Flagged: explicit tag';
    }

Replace with:
    function badgeLabel(reason) {
      const r = (reason || '').toLowerCase();
      if (r.includes('explicit')) return 'Flagged: explicit tag';
      if (r.includes('profanity') || r.includes('language')) return 'Flagged: strong language';
      if (r.includes('drug')) return 'Drug reference';
      if (r.includes('sexual')) return 'Sexual content';
      if (r.includes('adult')) return 'Flagged: adult themes';
      return 'Flagged: explicit tag';
    }

Do NOT modify setEvalBadge — per D-10 scope boundary.
Do NOT modify EVAL_BADGE_CLASS or EVAL_BADGE_LABEL objects.
  </action>
  <verify>
    grep -n "r.includes('drug')\|r.includes('sexual')" web_ui/templates/index.html
    — must show 4 lines (2 in setBadgeClass, 2 in badgeLabel)
    grep "badge--drug-reference" web_ui/templates/index.html → must appear in setBadgeClass return
    grep "'Drug reference'" web_ui/templates/index.html → must match exactly
    grep "'Sexual content'" web_ui/templates/index.html → must match exactly
    grep "setEvalBadge" web_ui/templates/index.html → must still exist and be unchanged (no drug/sexual mentions inside it)
  </verify>
  <acceptance_criteria>
    - web_ui/templates/index.html contains `if (r.includes('drug')) return 'badge--drug-reference';`
    - web_ui/templates/index.html contains `if (r.includes('sexual')) return 'badge--sexual-content';`
    - web_ui/templates/index.html contains `if (r.includes('drug')) return 'Drug reference';`
    - web_ui/templates/index.html contains `if (r.includes('sexual')) return 'Sexual content';`
    - Branch order in setBadgeClass: explicit → profanity → drug → sexual → adult → fallback (grep lines appear in this order)
    - Branch order in badgeLabel: explicit → profanity → drug → sexual → adult → fallback (grep lines appear in this order)
    - setEvalBadge function body does NOT contain 'drug' or 'sexual' (per D-10 scope boundary)
    - Existing branches for explicit, profanity, adult, and their return values are unchanged
    - badgeLabel returns 'Drug reference' (no "Flagged:" prefix, per D-07 and SC-01)
    - badgeLabel returns 'Sexual content' (no "Flagged:" prefix, per D-08 and SC-02)
  </acceptance_criteria>
  <done>setBadgeClass and badgeLabel each have drug and sexual branches in the correct position (explicit → profanity → drug → sexual → adult → fallback). Badge labels match success criteria text verbatim.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of new badge variants</name>
  <files>web_ui/templates/index.html</files>
  <action>
Human opens the dashboard and verifies visual appearance of the two new badge variants using the DevTools steps in how-to-verify. No code changes — this task confirms Tasks 1 and 2 produced the correct visual output.
  </action>
  <what-built>
    Two CSS badge classes (badge--drug-reference in purple, badge--sexual-content in pink/magenta) and two detection branches in setBadgeClass/badgeLabel. The skip feed will now show "Drug reference" and "Sexual content" badges for those skip reasons.
  </what-built>
  <how-to-verify>
    1. Open the dashboard in a browser (or view the HTML file directly).
    2. Open browser DevTools console — confirm no JS errors on page load.
    3. In DevTools console, paste and run:
       console.log(setBadgeClass('drug_reference'));   // expect: badge--drug-reference
       console.log(setBadgeClass('sexual_content'));   // expect: badge--sexual-content
       console.log(setBadgeClass('profanity'));        // expect: badge--profanity (unchanged)
       console.log(setBadgeClass('explicit'));         // expect: badge--explicit (unchanged)
       console.log(setBadgeClass(''));                 // expect: badge--explicit (fallback unchanged)
       console.log(badgeLabel('drug_reference'));      // expect: Drug reference
       console.log(badgeLabel('sexual_content'));      // expect: Sexual content
    4. Confirm badge--drug-reference renders as purple and badge--sexual-content renders as pink/magenta
       (inject a test badge via DevTools: document.body.innerHTML += '<span class="badge badge--drug-reference">Drug reference</span><span class="badge badge--sexual-content">Sexual content</span>')
  </how-to-verify>
  <verify>Human confirms: (a) no JS console errors, (b) setBadgeClass/badgeLabel return expected values, (c) badge colors visually correct</verify>
  <done>Human types "approved" — all DevTools checks pass with no JS errors and correct colors.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
grep -n "badge--drug-reference\|badge--sexual-content" web_ui/templates/index.html
  — must show: CSS class definition, setBadgeClass return, badgeLabel return (at least 3 matches per variant)

grep "rgba(130, 80, 190" web_ui/templates/index.html && grep "rgba(190, 80, 140" web_ui/templates/index.html
  — both must match

grep -c "r.includes('drug')" web_ui/templates/index.html
  — must be 2 (one in setBadgeClass, one in badgeLabel)

grep -c "r.includes('sexual')" web_ui/templates/index.html
  — must be 2 (one in setBadgeClass, one in badgeLabel)

grep "setEvalBadge" web_ui/templates/index.html | grep -c "drug\|sexual"
  — must be 0 (setEvalBadge not modified)
</verification>

<success_criteria>
1. Drug reference skip → "Drug reference" badge (purple, badge--drug-reference) in skip feed
2. Sexual content skip → "Sexual content" badge (pink/magenta, badge--sexual-content) in skip feed
3. Existing badges (explicit, profanity, adult) unchanged — all existing branch conditions and return values identical to pre-phase state
4. No JS errors on page load with pre-v1.3 entries (string-match fallback handles all unknown reasons safely)
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-badge-variants/13-01-SUMMARY.md`
</output>
